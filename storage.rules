rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Client uploads are disabled. Future backend paths require privacy review.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
