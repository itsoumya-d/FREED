rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Recovery data is local-only. Deployed Admin SDK code bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
