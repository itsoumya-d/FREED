# FREED Completion Audit

Objective: build a world-class porn recovery and adult-content blocking mobile app for iOS and Android by inspecting the provided prototype/design files, researching the mandatory references, implementing the product, and verifying it in the current workspace.

Status: not complete. The working app layer, durable local recovery state with private slip logging, Android native scaffold, iOS Screen Time extension targets, iOS simulator build, Android native debug build, simulated blocker flows, native monetization SDK wiring, and monetization integration boundary exist, but production-grade native blocking validation, live store/ad sandbox evidence, production AI backend validation, and device performance validation are not finished.

`docs/prompt-to-artifact-checklist.md` maps the original prompt requirements to concrete files, commands, evidence gates, and remaining blockers.

## Evidence Checklist

| Requirement | Evidence | Status |
| --- | --- | --- |
| Read existing prototype/design files and app name | `freed.html`, `ios-frame.jsx`, `freed-v2/*.jsx`, `freed-v2/tokens.*`, `ref/*.png`, `ref2/*.png` inspected. App name remains FREED. | Done |
| Match design language | `src/constants/design.ts` and `src/features/freed-app.tsx` use the existing dark wellness palette, rounded card system, floating pill navigation, streak visuals, panic/intervention surfaces, and premium paywall style. | Done for app layer |
| Research Google Play reference app | Listed with source in `docs/research-and-architecture.md`. | Done |
| Research Mobbin QUITTR flow | Mobbin URL listed, plus local `ref`/`ref2` screen exports used as full visual reference because unauthenticated live access is limited. | Done with limitation |
| Research blocker competitors, recovery psychology, platform constraints | Sources and synthesis are in `docs/research-and-architecture.md`. | Done |
| Product architecture | `docs/research-and-architecture.md` covers onboarding, home, shield, relapse interruption, challenge engine, CLARA, data model, monetization, privacy, and testing plan. | Done |
| Privacy data map | `docs/privacy-data-map.md` maps onboarding answers, streak/challenge history, check-ins, blocked attempts, accountability, notifications, protection permission setup status/copy, CLARA route inputs, challenge route inputs, optional disabled-by-default coarse weather context, server-only secrets, and future store-review disclosure requirements to concrete files. | Done for current local-first build |
| Mobile app scaffold | Expo Router app files: `app/_layout.tsx`, `app/index.tsx`, `package.json`, `app.json`, `tsconfig.json`. | Done |
| Onboarding, paywall, and permission setup | Implemented in `src/features/freed-app.tsx`. The flow now runs recovery-goal onboarding, then an explicit app-shield selection screen, then the paywall, then the protection setup screen. `RecoveryState` separately persists onboarding completion, paywall presentation, and platform-scoped protection activation, so a restart after app selection or paywall returns to the unfinished paywall/setup step instead of entering the dashboard early. When iOS/Android later reports that required native setup readiness was revoked, including adult filtering, selected app-limit scheduling, or Android app-timer configuration, FREED clears the saved activation and routes dashboard-style screens back to setup instead of treating old activation as proof. Android app timers persist only the supported packages the user selects in that app-selection step or later in Shield; iOS users are told the actual app/site selection happens through Apple's Screen Time picker during permission setup. The setup screen uses `src/lib/protection-permissions.ts` to show a platform-specific checklist for iOS Screen Time, ManagedSettings adult filtering, Safari Content Blocker rules, iOS app/domain shield selection, Android DNS-only VPN Guard, Android native adult-domain feed sync, Android Accessibility, supported doomscroll app packages, optional local notifications, and on-demand challenge verification. Each row explains the permission reason and data boundary, including no continuous screen capture, no OCR, no packet inspection, no full-traffic proxying, and camera/motion/steps/location only when a challenge requires verification. The setup screen now includes a Test Protection step that retries native adult-domain feed sync, checks local adult-domain blocking, normal-site allow behavior, and a freshly refreshed native permission/feed status sample without saving a blocked attempt; the completion CTA only becomes Activation Complete when that test passes while the current native permission/feed readiness is still true, and iOS/Android setup does not continue into the main app until activation is complete. Release evidence now requires `permissionSetupWizard` proof with the exact `onboarding-goals>app-selection>paywall>protection-explanation>permission-setup>test-protection>activation-complete` flow order, the production `permissionExplanationSummary` covering selected-app/site monitoring, known adult-domain blocking, and harmful site/search/app-limit threshold recovery challenges, and Test Protection passing on real hardware. | Done locally; physical-device permission walkthrough evidence missing |
| Main recovery dashboard | Implemented in `HomeScreen` within `src/features/freed-app.tsx`. | Done |
| Adult-only blocker classifier and feed operations | Implemented in `src/lib/blocking-engine.ts`. `src/lib/adult-domain-feed-ingestion.ts` adds server-side reviewed source ingestion from `FREED_ADULT_DOMAIN_FEED_SOURCE_URLS`, source URL safety checks, reviewed source-family IDs for OISD NSFW, StevenBlack, Cloudflare Family exports, and FREED-owned custom feeds, normal-browsing domain rejection/reporting, source fetch reports, merged feed creation with embedded seeds plus server-reviewed extras, a bounded `FREED_ADULT_DOMAIN_FEED_CACHE_TTL_SECONDS` in-process cache with in-flight reuse so reviewed sources are not refetched on every device sync, per-source `FREED_ADULT_DOMAIN_FEED_SOURCE_TIMEOUT_MS` and `FREED_ADULT_DOMAIN_FEED_SOURCE_MAX_BYTES` protection, and `stale-if-error` serving so an expired cache does not collapse to a smaller feed when every reviewed source refresh fails. `scripts/lib/adult-domain-feed-source-contract.js` mirrors the source-family parser for release preflight and self-tests malformed lines, duplicate IDs, and unreviewed source IDs before production envs can pass. `app/api/adult-domain-feed+api.ts` publishes the normalized feed with readiness, sanitized public source reports, ingestion/cache metadata, quoted `ETag`/`X-FREED-Adult-Feed-Checksum`/`X-FREED-Adult-Feed-Cache`/`X-FREED-Adult-Feed-Source-Max-Bytes` headers, CORS support for conditional checksum headers, 304 unchanged-checksum handling before Safari rule rebuilds, Safari content-blocker JSON export, and sanitized feed-version publication metadata on fresh cache misses. `src/lib/adult-domain-feed-sync.ts` now sends the conditional checksum as `If-None-Match`/`X-FREED-Adult-Feed-Checksum` only after required native/Safari layers are populated with matching checksums, treats 304 as a remote-cache hit, bounds the device-side remote feed request with `EXPO_PUBLIC_ADULT_DOMAIN_FEED_SYNC_TIMEOUT_MS` before falling back to the embedded feed, skips unchanged native/Safari rewrites, and only refreshes optional iOS matched-domain DNS settings when the active DNS profile no longer matches the requested count/provider. `modules/freed-protection/ios/FreedProtectionModule.swift` rejects empty, oversized, malformed, or non-blocking Safari content-blocker rule payloads before writing shared app-group rules, and `ios/FREEDSafariContentBlocker/ContentBlockerRequestHandler.swift` validates the shared rule file before serving it, falling back to the bundled seed `blockerList.json` when the shared file is missing or invalid. `src/lib/adult-domain-feed-publication.ts` can upsert version/checksum/domain count/Safari rule count/source reports/readiness into Supabase `adult_domain_feed_versions` through server-only credentials without storing the full domain list or source query tokens. The embedded fallback now covers 46 adult-domain seeds across TypeScript, Android native classification, and the bundled Safari Content Blocker, with core tests proving the static Safari fallback exactly matches the compiled embedded feed plus short-form web rules. `npm run smoke:adult-domain-feed` now validates the deployed feed JSON, cache/checksum headers, reviewed sanitized source reports, conditional 304 sync, and Safari content-blocker export. `scripts/classifier-safety-corpus.ts` and `npm run audit:classifier` now exercise 49 normal-browsing, recovery-research, overmatch-guard, adult-search, and adult-domain cases, including accountability, relapse-prevention, urge-support, sobriety, and nofap recovery intent while still blocking mixed free/consumption searches; current result: 49 pass, 0 fail. `npm run audit:android-classifier` compares Android `FreedUrlClassifier` source tables and key rule-order semantics against the TypeScript classifier, including host-storage normalization and focused-search fallthrough after invalid host candidates; current result: 15 pass, 0 fail. | Done for classifier and feed scaffold; production source-review ops and device evidence missing |
| Allow normal websites | `DEFAULT_ALLOWED_NORMAL_DOMAINS` includes Google, YouTube, Instagram, X/Twitter, social, education, gaming, streaming, productivity examples. Unknown domains default allow when no adult signal is found. Adult-looking hostname tokens are also allowed when paired with recovery/filtering context such as addiction, recovery, blocker, research, health, or support. Verified by `npm run test:core`. | Done for classifier |
| Adult search intent on safe sites | `classifyUrl` blocks adult consumption intent on allowed search hosts, known search engines, and regional Google domains while allowing recovery, health, blocker, and education searches that mention adult terms without consumption intent. Android `FreedUrlClassifier` mirrors the same behavior and the recovery-context hostname token exception. Verified by `npm run test:core`, `npm run audit:android-classifier`, and `android/gradlew :freed-protection:compileDebugKotlin`. | Done for classifier |
| Free relapse interruption flow | Browser smoke test verified adult-content intercept -> rewarded reset -> challenge -> completion. Self-triggered Panic Button/urge mode now creates a privacy-safe self-urge intervention and skips the ad so help is immediate. The shared intervention screen now uses source-aware copy: adult sites, explicit searches, selected app/short-form shields, iOS Screen Time shields, and self-reported urges are not all framed as adult-site detection. | Done for prototype |
| Premium relapse interruption flow | Browser smoke test verified premium skips ad and starts challenge. | Done for prototype |
| Adaptive challenge system | `src/lib/recovery-engine.ts` and `src/data/challenge-templates.ts` score challenges by streak, mood, attempts, time/day-part, weekend context, premium status, onboarding reset preferences, private slip count/window/trigger summaries, coarse intervention context, local urge forecast level, check-in energy/urge/sleep signals, real location-permission state, coarse real-weather context when available, user-created premium custom resets, and recent challenge outcomes. `src/lib/challenge-generator.ts` adds a privacy-safe remote generation boundary that sends only recovery signals/history plus aggregate slip count/window and coarse trigger categories, coarse surface/category context, aggregate forecast fields, and explicit context signals, normalizes generated challenges, strips link/domain echoes from visible fields and generated IDs, filters premium-only output for free users, preserves local custom resets in the returned set, and falls back to the deterministic set if remote generation is missing, invalid, or failed. `src/lib/challenge-verification.ts` maps generated/local challenges to timer, accelerometer motion, pedometer step, foreground-location movement, camera photo, or connection-action requirements, and `src/features/freed-app.tsx` keeps completion locked until live evidence satisfies the requirement. Outdoor movement verification now requires multiple accurate foreground location fixes before distance can unlock, so a noisy GPS jump cannot satisfy the challenge by itself. Connection challenges now require an explicit connection action but do not become impossible when no accountability partner is configured; if a partner exists, FREED opens that handoff, otherwise the user can record that they reached out. Photo challenges open a fresh camera capture with `expo-image-picker`, then call the native `FreedProtection.classifyChallengePhoto` bridge; iOS uses Vision `VNClassifyImageRequest`, and Android uses ML Kit on-device image labeling. `app/api/challenges+api.ts` adds a server route scaffold that reads `OPENAI_API_KEY` or `GEMINI_API_KEY` only from server env through `src/lib/server-ai-provider.ts`, requests structured recovery challenges with timeout-bounded provider calls when configured, sanitizes output before display/storage, and returns local safe fallback challenges if the backend is missing or fails. Completion feedback records whether the urge passed or the user still needs support, then biases future recommendations toward interventions that helped. Verified by `npm run test:core`, including preference-derived challenge category ranking, custom reset persistence/selection/no-remote-leakage, slip-pattern ranking, privacy-safe intervention/context/forecast-signal adaptation, challenge verification, native photo-classifier wiring, remote-generator fallback tests, remote output sanitization tests, and server-route fallback tests. Physical-device sensor/camera QA is still missing. | Partial |
| Discipline configuration | `src/lib/recovery-state.ts` persists daily limit, configurable short-form interruption threshold, earned unlock duration, opt-in Android doomscroll app packages with primary-package storage plus alias expansion, challenge intensity, outdoor/exercise/social frequencies, emergency strict mode, sleep mode, deep focus, work hours, weekend mode, editable sleep/work start/end times, and an earned-unlock ledger with expiry timestamps. `src/features/freed-app.tsx` surfaces these controls in the Shield tab, including `HH:MM` schedule inputs for sleep and work windows, records bounded earned unlocks after verified challenge completion while separately saving "helped" or "still-urging" outcome feedback, caps recorded and hydrated earned unlocks to 120 minutes, syncs only native-shield-relevant earned windows plus the redacted source host to the native bridge, mode-expanded Android app packages including regional TikTok aliases, the configured daily app budget, and the short-form threshold to native protection, and syncs enabled sleep mode into the iOS Screen Time Night Guard schedule using the configured minute-level sleep window. iOS clears selected app shields only for Screen Time shield-sourced earned windows while keeping adult web filtering active, treats Screen Time shield handoffs as opaque app-shield interventions with unknown category instead of browser/adult events, and the bridge/DeviceActivity extension reapply selected shields after expiry only when a Night Guard risk window or selected-target daily limit is active; Android keeps an event-driven same-day foreground usage ledger, cross-checks user-enabled UsageStats totals for restart-resilient app limits, interrupts user-selected packages, or the full known doomscroll package set when emergency strict, sleep, deep focus, work-hours, or weekend mode is active, only after the configured daily limit is reached while no earned unlock is active, stores the inferred source package for bounded app/short-form unlocks, schedules a bounded source-aware relock if that protected app remains foreground after expiry, waits for sustained YouTube Shorts, Instagram Reels, or TikTok For You short-form behavior before short-form handoff, gates YouTube/Instagram/TikTok scroll thresholds behind selected/focused Shorts/Reels/For You surface evidence so broad app scrolling is less likely to trigger a short-form intervention, requires the selected short-form surface to still be visible when the threshold timer fires, covers TikTok across `com.zhiliaoapp.musically`, `com.ss.android.ugc.trill`, and `com.tiktok`, and keeps adult-domain filtering protected. Browser/adult-domain and panic challenge completions remain local challenge windows instead of pausing native app shields. Release evidence now requires concrete earned-unlock allow, rejected non-Screen-Time source, and automatic relock artifacts on iOS plus allow/relock artifacts on Android, so temporary unlocks must prove they come from the right native source, cannot be minted from browser/adult challenges, and close again after expiry. `buildChallengePreferenceSignal`, `src/data/challenge-templates.ts`, `src/lib/challenge-generator.ts`, and `app/api/challenges+api.ts` use the settings as privacy-safe challenge preference signals, not as raw browsing history. Verified by `npm run test:core` and release gate `discipline-configuration-contract`. Native timed unshielding/relock, sleep scheduling, app-shield behavior, and short-form threshold behavior still require production entitlement/device validation and store/ad sandbox evidence. | Partial |
| Streak, motivation, analytics | Implemented in home, analytics, profile, progress surfaces in `src/features/freed-app.tsx`. `src/lib/recovery-state.ts` now derives recovery XP, level progress, and achievement badges from local streaks, check-ins, protected attempts, completed/helpful challenges, and honest reset logs; Analytics surfaces the current level and top badges without shame language. App-open and foreground-duration analytics are derived from real hydrated AppState sessions: the app starts a session immediately when the hydrated runtime is already active, closes sessions on inactive/background/unmount, and bounds stale open sessions instead of inventing fake duration. Profile also exposes a private non-shaming slip log that preserves the previous streak and restarts the active streak. | Done for prototype |
| AI recovery coach | `CoachPanel` UI, `generateCoachReply` local pattern-aware replies, `src/lib/ai-coach.ts` privacy-safe remote-provider boundary, and `app/api/clara+api.ts` server route scaffold exist. CLARA is reachable from Library and after a challenge marked "Still Need Support." Remote mode redacts URLs/domains from user text, sends only host-level recent risk summaries, slip count/window and coarse trigger categories, and guardrails, sanitizes remote provider output before display, calls a configured provider/endpoint when available, and falls back locally on missing config or failure. The server route reads `OPENAI_API_KEY` or `GEMINI_API_KEY` only from server env through `src/lib/server-ai-provider.ts`, prefers OpenAI when configured, preserves Gemini as a fallback provider, bounds provider requests and JSON reads with `FREED_AI_PROVIDER_TIMEOUT_MS`, sanitizes remote model text, and returns local fallback support if the key/provider fails. Crisis/self-harm language stays local/server-safe and returns immediate safety resources. `src/lib/endpoint-safety.ts` requires production AI endpoints to be valid HTTPS non-local URLs with concrete route paths, and rejects URL credentials, query strings, fragments, private-network addresses, reserved documentation hosts, and placeholder hosts; `npm run preflight:release-env`, `npm run audit:release`, and `npm run smoke:ai-backend` enforce that. `npm run eval:ai-safety` exercises CLARA redaction, crisis-local routing, no-shame fallback language, bounded route response reads, API fallback non-echo behavior, and challenge privacy contracts. `npm run smoke:ai-backend` is available for deployed endpoint validation, reports `configured-ai-model`, exercises `challenge-personalization-profiles` across free high-risk and premium/steady profiles with context and aggregate urge-risk forecast signals, can write a sanitized `ai-backend-smoke-v1` report with `--report`, and fails until remote CLARA/challenge endpoints and a concrete `OPENAI_MODEL` or `GEMINI_MODEL` are configured. `docs/validation/evidence/ai-backend-smoke.json` is now required to prove deployed smoke, sanctioned `npm run preflight:release-env` command proof with run ID/artifact, safety eval case/failure counts and artifacts, endpoint smoke pass/failure counts plus run IDs/artifacts, configured model match, challenge personalization profile count/artifact with aggregate forecast profile proof and zero premium-only free challenges, no-sensitive-echo sample counts plus run ID/artifact, redaction report artifact, crisis fallback artifact, and provider fallback artifact, while rejecting raw prompts, private notes, transcripts, unredacted responses, sensitive URLs/domains, and provider API keys. Production still needs deployed endpoint env configuration, backend safety review, and live model validation evidence. | Partial |
| Retention features | Daily streak, next-milestone/celebration card, pledge shortcuts, panic/urge mode, analytics, persisted daily check-ins, mood/urge/sleep tracking, reflection capture, private slip logging, check-in-based insights, local weekly recovery reports, premium monthly growth reports, learned risk window/best-reset/momentum signals, opt-in local recovery reminders, and a smart guard reminder that shifts timing before learned risk/slip windows exist. `src/lib/urge-risk-forecast.ts` now derives a local urge forecast from real check-ins, protected attempts, slips, challenge outcomes, and discipline settings without raw browsing, notes, contacts, or location. `src/lib/retention-orchestrator.ts` builds a privacy-safe daily retention request from aggregate recovery signals plus redacted forecast fields only, generates a local next-best-action plan, supports a production-safe remote provider/endpoint with sanitized output, hard request/JSON timeouts through `src/lib/remote-provider-timeout.ts`, and local fallback, and `app/api/retention+api.ts` adds the timeout-bounded server OpenAI-or-Gemini-backed retention route scaffold. Profile surfaces the resulting Daily Recovery Plan card. Remote push dispatch has a server-authorized FCM/APNs boundary with preset recovery-safe copy, but production campaigns still require credentials, provider smoke evidence, privacy review, and explicit enablement. | Partial |
| Monetization | Free vs premium state, verified-plan metadata, plan-specific capability mapping, paywall, rewarded reset simulation, premium no-ad path, restore purchase entry point, individual/family/accountability/AI-coach product IDs, and a `src/lib/monetization.ts` abstraction with mock/native modes are implemented. Verified purchase/restore results persist `premiumPlanId`, audience, and activation timestamp when the store/provider payload identifies the product; legacy premium records stay active without inventing a plan. `getPremiumCapabilities` maps paid plans to no-ad interruptions, premium resets, advanced analytics, CLARA, custom challenges, sponsor/accountability, family support, relapse prediction, and deep reports; the app now uses those capabilities for ad skipping, premium challenge access, local custom reset creation, CLARA context, analytics detail, urge forecast access, monthly deep reports, sponsor reports, family/support-circle report handoff, and Profile membership chips instead of treating every paid state only as a boolean. The monetization module exposes a native provider contract for store purchases, restore, rewarded session creation, and rewarded completion, resolves direct native IAP, optional RevenueCat fallback, AdMob, and product-ID environment configuration, and reports native-mode readiness gaps before release, including hard failures for missing/unsafe purchase verification endpoints, missing server-only App Store / Google Play verification credentials, non-production App Store Server API mode, AdMob test ads, placeholder RevenueCat keys when RevenueCat mode is selected, malformed AdMob app IDs, malformed rewarded-unit IDs, and Google's sample AdMob publisher ID. `expo-iap`, `react-native-google-mobile-ads`, and optional fallback `react-native-purchases` are installed. `src/lib/native-iap-adapter.ts` adds the preferred StoreKit 2 / Google Play Billing adapter and fails closed unless the configured timeout-bounded server purchase-verification endpoint confirms the entitlement. `app/api/purchases/verify+api.ts` plus `src/lib/purchase-verification.ts` add a server-side timeout-bounded App Store / Google Play verification boundary that can mint short-lived App Store Server API JWTs and Google Play OAuth tokens from server-only credentials, returns only sanitized entitlement state, validates all configured FREED premium/family/accountability/coach products, and never echoes raw receipts or purchase tokens. `src/lib/native-monetization-adapter.ts` adds RevenueCat-style fallback and AdMob-style rewarded adapter factories, infers restored plans from verified product identifiers when present, and `src/lib/native-monetization-runtime.ts` prefers `expo-iap` before falling back to RevenueCat. `app.config.js` injects the Google Mobile Ads config plugin only when platform AdMob app IDs are production-formatted and not Google sample IDs, while static `app.json` no longer carries an ignored root-level Google Mobile Ads block. Android `./gradlew :app:assembleDebug` passes with the native monetization packages installed, and iOS `pod install --repo-update` plus simulator `xcodebuild` pass with `Google-Mobile-Ads-SDK 13.1.0`, `RevenueCat 5.70.0`, `RNGoogleMobileAds 16.3.3`, and `RNPurchases 10.1.0`. Tests prove configured providers/adapters can grant entitlements/rewards while missing, placeholder, malformed, sample, rejected, or failing providers fall back/report blockers without blocking recovery. Store/ad evidence now requires sanctioned `npm run preflight:release-env` command proof with run ID/artifact, concrete free rewarded intervention proof with `freePostAdChallengeLatencyMs<=5000`, and concrete premium no-ad intervention proof with `premiumNoAdLatencyMs<=3000`, so release proof covers both free and premium relapse paths. Real App Store/Play product setup, live purchase-verification credentials/evidence, optional RevenueCat fallback validation, live free rewarded proof, live premium no-ad proof, and live ad-network sandbox validation are not complete yet. | Partial |
| Accountability features | Local privacy-first partner and support-circle setup is implemented in `src/lib/recovery-state.ts`, `src/lib/accountability.ts`, and `src/features/freed-app.tsx`. Users can opt in, choose SMS/email, store a partner template locally, and message the partner from relapse interruptions or connection challenges without needing a paid plan, preserving urgent support. Aggregate weekly sponsor report sending is gated by sponsor/accountability-capable plans through `getPremiumCapabilities`; family/accountability-capable plans can store local support-circle contacts and open per-contact SMS/email report handoff by explicit user action. The report itself still contains only streak, protected moments, resets, check-ins, slip count, risk window, and focus. Sponsor/support-circle reports redact URLs/domains and exclude private notes, daily reflections, support contacts, and browsing details from the report body. `src/lib/community-safety.ts` adds a fail-closed remote-community readiness gate that requires explicit opt-in, consent versioning, privacy disclosure review, community guidelines acceptance, moderation queue readiness, abuse-reporting SLA, block/mute controls on every surface, disabled direct messaging/user-generated post text, short retention plus deletion review, crisis-escalation review, and aggregate-only payload rules before any future remote community workflow can send a milestone. Remote community workflows are still not implemented. | Partial |
| Analytics and personalization | Local scoring, XP/level/badge derivation, day-scoped attempt tracking, real app-session opens/foreground duration, typed recovery state hydration, private slip records with previous-streak/trigger/note fields, challenge history with outcome feedback, onboarding-derived challenge category preferences, daily check-ins, generated recovery insights, local weekly recovery reports, premium monthly growth reports, and adaptive on-device signals for risk window, slip window, slip trigger, best-performing reset, and risk momentum are implemented in `src/lib/recovery-state.ts` and surfaced in Analytics/Profile. `src/lib/recovery-analytics.ts` builds an aggregate-only `aggregate-v5` analytics snapshot with protected-moment counts, self-reported urge counts, browser/search/manual/app interception counts, a compact blocked-attempt source breakdown, app opens, foreground minutes, earned unlock totals, weekly unlock frequency, seven-day streak history with relapse-reset counts, relapse resets, relapse reset rate, days since last reset, full 24-hour urge/risk histograms, challenge help/success rate, category-level challenge success, recovery score, mood/sleep/urge aggregates, coarse trigger categories instead of raw private trigger text, redacted pattern labels, and privacy flags; the Analytics screen keeps basic score/snapshot/report surfaces available locally while gating adaptive-signal detail behind advanced analytics capability and monthly growth detail behind deep-reports capability. `sendGatedAnalyticsPayload` now implements opt-in remote analytics transport and refuses to send until explicit current-version consent, a production-safe HTTPS `/api/analytics` endpoint that still matches the currently configured build endpoint, aggregate-only sharing, short retention, and no private-note/browsing/support-contact sharing are satisfied; the actual POST is bounded by `EXPO_PUBLIC_ANALYTICS_TIMEOUT_MS` so opt-in sharing cannot hang the app. `app/api/analytics+api.ts` plus `src/lib/recovery-analytics-ingestion.ts` enforce the same aggregate-only boundary server-side, rate-limit writes through the shared hashed backend limiter, reject stale analytics consent versions plus incomplete streak-history/hourly/category aggregate series and raw attempts/URLs/domains/notes/contacts/transcripts/receipts, and can write sanitized snapshots to Supabase through server-only credentials with `FREED_ANALYTICS_SUPABASE_TIMEOUT_MS` protecting stalled Supabase inserts while preserving `data_retention_days` plus `received_at` for the generated database expiry; `scripts/analytics-ingestion-smoke.ts` validates the deployed route with a one-day aggregate-v5 smoke snapshot plus raw private-field rejection and no response echo before release; `src/lib/recovery-backup-sync.ts` and `app/api/recovery-backup/sync+api.ts` add an authenticated hosted-continuity boundary that accepts only encrypted backup envelopes, stores hashed user/device identifiers, and never receives passphrases or decrypted recovery state; `src/lib/backend-provider-timeout.ts` bounds shared Redis/Supabase provider calls through `FREED_BACKEND_PROVIDER_TIMEOUT_MS`; `src/lib/backend-retention-cleanup.ts` and `app/api/backend/cleanup+api.ts` add secret-authorized expired-row cleanup for aggregate analytics, feed metadata, encrypted backups, purchase audits, and redacted AI events without selecting row payloads; `docs/backend/supabase-schema.sql` and `supabase/migrations/20260518000100_freed_backend_core.sql` now define the constrained Supabase/PostgreSQL tables for aggregate analytics, reviewed adult-domain feed versions, encrypted recovery backups, sanitized purchase verification events, redacted AI backend events, and backend job runs. `src/lib/backend-architecture.ts`, `app/api/backend/readiness+api.ts`, and `scripts/backend-architecture-audit.js` add a no-secret backend readiness contract covering Supabase, Redis/Upstash, AI, purchases, optional hosted backup sync, maintenance cleanup, and FCM/APNs notification dispatch credentials without returning secret values; FCM readiness now matches runtime by requiring `FIREBASE_PROJECT_ID` or a Firebase service-account `project_id` when using FCM v1 access-token or service-account credentials. `src/lib/urge-risk-forecast.ts` adds a real-data-only local forecast from check-ins, attempts, slips, challenge outcomes, and discipline settings, and the Analytics forecast card is shown only for relapse-prediction-capable plans. Challenge generation, CLARA, and retention orchestration receive only privacy-safe aggregate slip/forecast summaries with coarse trigger categories when available, while slip notes, raw slip triggers, daily reflections, accountability contacts, and raw browsing details stay local. Blocked attempt storage now redacts raw paths/query strings to host-level URLs, and hydration redacts legacy raw attempt URLs. Remote analytics sharing and hosted backup sync remain disabled by default at runtime; production release still requires a deployed analytics ingestion endpoint, FCM/APNs dispatch credentials, product/privacy disclosure review, and production Supabase/Auth evidence before enabling those flows. | Partial |
| Backend analytics privacy-key hardening | `src/lib/recovery-analytics-ingestion.ts`, `docs/backend/supabase-schema.sql`, `supabase/migrations/20260518000100_freed_backend_core.sql`, and `supabase/migrations/20260520000100_harden_analytics_privacy_keys.sql` now reject normalized sensitive analytics keys such as `private_notes`, `rawURL`, `purchase_token`, and `receiptData` at both API and database layers. | Done locally; production still requires migration application proof plus deployed analytics smoke evidence |
| Privacy safety audit | `npm run audit:privacy` checks local-first privacy, iOS Complete Data Protection, Android permission/backup boundaries, server-only secrets, recovery-data redaction, aggregate analytics, explicit permission copy, the Android AccessibilityService/DNS Guard disclosure, the iOS Screen Time/Safari review pack, and on-device challenge-verification boundaries. | Done for current local-first build |
| Accessibility safety audit | Shared `PillButton` controls now expose button role, label, hint, and disabled state; onboarding answers and premium plans expose radio semantics and selected state; bottom navigation exposes named tabs; and the urgent-support action is announced as immediate urge support. `scripts/accessibility-safety-audit.js` plus `npm run audit:accessibility` enforce the current contract, and `npm run verify:release` includes it before privacy/performance gates. Current result: accessibility audit 6 pass, 0 fail. | Done for key app controls |
| Dependency security audit | `npm run audit:dependencies` runs `scripts/dependency-audit.js`, which wraps real `npm audit --omit=dev --json` with a sanitized `freed-dependency-audit-v1` report surface for release readiness. `package.json` pins narrow `postcss@8.5.14` and `uuid@11.1.1` overrides for Expo Metro's transitive PostCSS advisory and the vulnerable transitive `xcode -> uuid` path without changing the Expo SDK. Current result remains 0 vulnerabilities when npm registry access is available; npm/tool/registry failures now stay explicit release blockers instead of unparseable drift. | Done for current dependency graph |
| Expo project health | Expo SDK 55 package versions are aligned, direct Expo peers required by installed packages are declared, `.expo/` is ignored, the invalid app-config `newArchEnabled` field was removed, and `app.config.js` owns dynamic production-only Google Mobile Ads plugin injection. Because this workspace intentionally owns `ios/` and `android/`, `package.json` disables only Expo Doctor's native config sync check while native build settings are maintained directly. Current `npx expo-doctor` result: 17/17 checks passed. | Done for current Expo/native workspace |
| Notification system | `expo-notifications` is installed and configured. `src/lib/recovery-reminders.ts` builds privacy-safe local daily reminder plans, derives smart guard timing from local weekly risk/slip windows, schedules/cancels native local notifications when enabled, and routes notification taps with `{ route: "checkin" }` into Daily Check-In. The native notification response listener now loads only after reminders are enabled, so simulator/first-run launches do not import native notification storage or show dev entitlement noise before the user opts in. The scheduler path is injectable for tests, and `npm run test:core` verifies stale IDs are canceled, Android channel setup occurs, daily triggers are scheduled, and notification payloads contain only route/kind metadata without streak, attempt, slip, reflection, URL, token, or private recovery data. Profile UI exposes opt-in morning, evening, and night-guard reminders plus the current smart timing explanation. Remote push now has a server-authorized FCM/APNs dispatch boundary with preset recovery-safe copy only; `scripts/remote-notification-smoke.ts` validates the deployed dispatch route without sending a push by checking unauthorized and private-field rejection, and can write a sanitized `remote-notification-smoke-v1` non-sending report without push tokens, dispatch secrets, custom copy, URLs/domains, private notes, contacts, or purchase tokens. Production campaigns still require credentials, provider smoke evidence, and review before enablement. | Partial |
| iOS Screen Time implementation | FamilyControls, ManagedSettings, and DeviceActivity provide selected-target limits and atomic shield handoff. Earned app/domain unlocks use exact category-policy exceptions so unrelated shields remain active and expiry restores the selection. Safari Content Blocker is adult-domain-only; the iOS 15.4+ MV3 Safari Focus Shield handles approved short-form web routes through a background-worker native relay and Universal Link. The shared client, UI, environment, and release gates expose no iOS NetworkExtension DNS Settings path. Full workspace build remains unverified because the bounded CocoaPods/workspace attempt did not complete. | Partial |
| Android Accessibility/DNS implementation | `modules/freed-protection/android` includes `FreedDoomscrollApps.kt` as the native package/rule/host contract consumed by both the AccessibilityService and module bridge, an AccessibilityService, Usage Access bridge, a Settings-displayed `FreedUsageAccessConfigActivity` rationale for aggregate selected-app timers, native adult-only classifier mirror, native adult-domain feed persistence through cached `FreedAdultDomainFeed`, supported-browser URL/search field extraction for stable and beta/dev/canary Chrome/Edge, Firefox variants, Brave, Opera, stable/beta Samsung Internet, DuckDuckGo, Vivaldi, and Kiwi, narrow focused-WebView context detection, field-gated focused/editable fallback scanning to avoid page-content false positives, raw focused browser search-text classification before navigation with a redacted `focused-search.app.freed.local` handoff for adult intent, opt-in configured or discipline-mode-expanded app package interruption for doomscroll apps after the configured same-day app usage budget is reached while no matching source-scoped earned unlock is active, configurable sustained YouTube Shorts, Instagram Reels, and TikTok For You short-form thresholding before `short-form:*` handoff with TikTok package coverage for `com.zhiliaoapp.musically`, `com.ss.android.ugc.trill`, and `com.tiktok`, service-enabled status detection, pending-intervention persistence, a no-history native `FreedInterventionActivity` for immediate supportive handoff before React opens, native handoff task cleanup (`excludeFromRecents`, no outside-touch dismissal, repeated-intent handling, `finishAndRemoveTask`), JS consumption into the relapse flow, and a DNS-only `FreedVpnService` fallback that routes configured DNS resolver IPs, parses IPv4/UDP DNS packets, classifies against the synced adult-domain feed, returns NXDOMAIN for classified adult domains, forwards allowed DNS upstream with primary/secondary failover, returns SERVFAIL instead of silently dropping allowed queries when every resolver fails, cleans up the foreground service/TUN descriptor when Android revokes VPN permission, persists user-enabled restart intent only after Android VPN consent is already granted, restores after reboot or app update only while that user intent and VPN consent remain valid, records the last restart action/result/skip reason, records the last successful resolver/failure for QA, records per-session query/blocked/allowed/SERVFAIL/malformed counts plus uptime, last session duration, and stop reason without storing normal browsing hosts, and runs as a typed foreground service with a persistent notification. The native module declares `PACKAGE_USAGE_STATS`, opens Android Usage Access settings, reports `usageStatsAuthorized`, exposes the Usage Access config activity/reason, unique observed selected-app package names, aggregate same-day selected-app foreground minutes, and a per-package minute map for QA without reading in-app content, and preserves the same feed, DNS, app-limit, Private DNS, DNS Guard lifecycle/restart, and earned-unlock diagnostics on permission/filter/app-shield/unlock action responses as `getStatus()`. The app-shield usage ledger is event-driven from Accessibility foreground-package changes, resets by local day, writes only on package transitions/day rollover, cross-checks user-enabled UsageStats totals so daily limits survive service restarts, scopes earned unlocks to the Android package that earned them, and avoids timer polling; short-form detection uses selected labels/resource IDs when available plus subscribed `typeViewScrolled` events, surface-gated bounded scroll cadence, current selected Shorts/Reels/For You confirmation at the threshold deadline, and a threshold timer rather than immediate interruption. The React app syncs the embedded or configured remote adult-domain feed into native protection at startup, uses conditional checksum requests so unchanged remote feeds keep the cached native list without a full rewrite, consumes pending native interventions on launch and when it returns to foreground, with a freshness window so stale native records do not trigger later; configured app attempts enter recovery history as `source: "app"` and aggregate-only analytics counts them separately. The Profile Native Protection card now exposes status, authorize/settings actions, configured app count, package-level Usage Access coverage, synced adult-domain feed version/checksum/domain count, Android Private DNS mode/specifier with a guided Network settings path, last DNS Guard resolver diagnostics, DNS Guard session counters/lifecycle/restart diagnostics, and Start/Stop DNS Guard controls when the platform reports local VPN fallback support. `SYSTEM_ALERT_WINDOW` is removed from app config and the merged debug manifest. The merged debug manifest contains `FOREGROUND_SERVICE_SPECIAL_USE`, `foregroundServiceType="specialUse"`, `PROPERTY_SPECIAL_USE_FGS_SUBTYPE`, `RECEIVE_BOOT_COMPLETED`, and a non-exported `FreedVpnAutostartReceiver` for `BOOT_COMPLETED` / `MY_PACKAGE_REPLACED` guarded by existing VPN consent and explicit user enablement. `docs/store-policy/android-accessibility-and-fgs-disclosure.md` now supplies the Play Console AccessibilityService declaration, in-app disclosure copy, Data safety mapping, and special-use foreground-service justification tied to the evidence fields. The Android project includes the `app.freed.qawebview` QA fixture module for focused-WebView validation, and the latest `./gradlew :freed-protection:compileDebugKotlin` run now passes from `android/` after the source-scoped earned-unlock update; no Kotlin source deprecation warnings remain, apart from the existing Gradle deprecation notice. Broad real-browser/WebView/app-shield/device VPN/feed-update testing, Play policy review submission/approval for AccessibilityService use, Play policy review submission/approval for the special-use foreground-service subtype, Usage Access walkthrough evidence, long-session lifecycle validation, and reboot/package-update restart validation on physical devices are not complete. | Partial |
| Performance protections | Static checks cover Android DNS-only VPN/resolver lifecycle, event-driven app shields, no-overlay recovery, no runtime screenshot/OCR polling, absence of iOS NetworkExtension paths, and the Safari Focus Shield background-worker contract. Physical battery, thermal, memory, routing, speed, and DNS evidence remains outstanding. | Partial |
| Durable recovery storage | `expo-sqlite` localStorage polyfill added. `src/features/freed-app.tsx` persists onboarding completion, quiz answers, verified premium entitlement status, verified premium plan metadata when known, protected attempts, self-urge panic interventions, completed challenges, challenge history, private slip records, and daily check-ins. Protected attempts are stored as redacted host-level URLs plus host/rule/timestamp metadata, not raw path/query URLs. `src/lib/recovery-backup.ts` adds opt-in passphrase encrypted backup/restore with PBKDF2-SHA256 and AES-GCM; the exported envelope does not expose private notes, reflections, accountability contacts, or adult hosts, and restore clears device-local notification IDs so reminders must be scheduled on the new device. `src/lib/recovery-backup-client-sync.ts`, `src/lib/recovery-backup-sync.ts`, `src/lib/supabase-auth-client.ts`, and `app/api/recovery-backup/sync+api.ts` add an optional hosted sync path for encrypted envelopes only: the app encrypts/decrypts locally, the client can request Supabase magic links or Apple/Google OAuth handoff with public anon credentials, the route requires authenticated Supabase user validation and rate limiting, and storage uses hashed identifiers. Profile surfaces an Encrypted Backup card and hides sync controls until `EXPO_PUBLIC_RECOVERY_BACKUP_SYNC_ENDPOINT` is configured. Static export smoke verified reload persistence for onboarding, blocked attempts, and check-ins. Production Supabase provider setup, redirect testing, privacy disclosure review, and device evidence are still required before hosted sync is enabled. | Done for local device storage plus encrypted backup; hosted sync boundary and optional auth controls scaffolded |
| Automated core tests | `tests/core.test.ts`, run with `npm run test:core`; now includes recovery-state persistence, encrypted backup round-trip/no-leak tests, hosted encrypted-backup client and server sync auth/no-leak tests, backend retention cleanup no-select/delete-only tests, bounded JSON request-body fail-closed tests, attempt day-scope tests, blocked-attempt URL/host/result-host redaction and legacy hydration redaction, panic intervention self-urge metadata, private slip-log hydration and streak reset semantics, daily check-in replacement, insight selection, weekly recovery report generation, learned risk window/best-reset/momentum signals, private slip trigger/window summary without shame language, XP/level/achievement badge derivation, aggregate-only analytics snapshot and fail-closed remote analytics sharing tests, backend architecture readiness/no-secret tests, adaptive challenge outcome and onboarding preference tests, dynamic challenge generator request/readiness/remote-fallback tests, CLARA coach redaction/readiness/crisis/remote-fallback tests, retention orchestration aggregate-only request tests, retention remote-provider fallback/sanitization tests, CLARA/challenge/retention API route fallback tests, accountability deep-link/state and sponsor-report no-leak tests, remote-community fail-closed consent/abuse gating and aggregate-only milestone tests, purchase verification route fail-closed/redaction tests, hashed purchase-verification audit tests, redacted AI backend audit tests, App Store/Google Play verification response tests, server-side App Store/Google Play auth-token minting tests, release env preflight pass/fail behavior for production store/ad/AI config including reviewed adult-feed source-family gating, validation evidence scaffold, draft-validation, and fail-closed promotion safety, reminder-plan validation, reminder-response route guard tests, lazy notification response listener gating, classifier safety corpus coverage, broad normal-browsing false-positive coverage, recovery/education search allowances, host-token overmatch guards, native no-overlay/DNS-only config safety assertions, iOS DeviceActivity schedule bridge assertions, Android focused/editable Accessibility fallback assertions, product ID stability, native-missing purchase fallback, native monetization config/readiness tests, native-IAP missing-server and rejected-server fail-closed tests, RevenueCat-style and AdMob-style adapter factory tests, native monetization provider contract tests, and rewarded-reset fallback tests. `npm run audit:smoke-harnesses` syntax-checks and self-tests the iOS and Android simulator/emulator smoke harnesses, the bounded iOS native-build helper, the shared PNG screenshot analyzer, evidence target/output/env-file/report-path safety validators, EAS submit guard, centralized Android doomscroll, web short-form, and adult-domain feed source contract helpers, iOS/Android physical-device evidence helpers, normal-browsing corpus matrix helper, performance profiling matrix helper, store/ad sandbox matrix helper, backend readiness smoke helper, Supabase schema smoke helper, adult-domain feed smoke helper, analytics ingestion smoke helper, remote notification smoke helper, purchase verification smoke helper, and AI backend smoke matrix helper, including crash/redbox/ANR parser, no-output xcodebuild timeout command construction, nonblank screenshot proof, placeholder/local/insecure capture-target rejection, no-secret backend readiness checks, deployed Supabase table/column checks, deployed adult-feed contract checks, draft/internal store-submit gating, and physical-device guard cases, before expensive release verification steps. `npm run eval:ai-safety` adds a focused AI safety harness for CLARA/challenge input redaction, remote output redaction, crisis routing, no-shame local copy, premium filtering, bounded route response reads, API fallback non-echo behavior, and challenge-route browsing-field exclusion. `npm run audit:backend` verifies the deployable Supabase migration, deployable Supabase Edge Function cron/admin contracts, Redis/no-secret backend contract, bounded public JSON request bodies, adult-feed, encrypted-backup sync, retention cleanup, purchase, notification, and AI audit persistence hooks. `npm run smoke:backend-readiness` validates the deployed no-secret readiness route and core backend readiness when configured and can write a sanitized `backend-readiness-smoke-v1` JSON report with `--report`, `npm run smoke:supabase-schema` validates the deployed Supabase REST table/column contract with service-role credentials, limit=0 reads, public anon lockout, and can write a sanitized `supabase-schema-smoke-v1` report when configured, `npm run smoke:adult-domain-feed` validates the deployed adult feed and Safari export when configured, `npm run smoke:analytics-ingestion` validates deployed aggregate analytics acceptance plus sensitive-field rejection when configured, `npm run smoke:remote-notifications` validates deployed notification dispatch rejection paths without sending a push when configured, `npm run smoke:ai-backend` validates deployed AI endpoints and can write a sanitized `ai-backend-smoke-v1` report when configured, and `npm run smoke:purchase-verification` validates the deployed purchase endpoint fails closed, redacts tokens, and can write a sanitized `purchase-verification-smoke-v1` report before store sandbox evidence can be trusted. | Done |
| Release validation evidence | `docs/validation/README.md` defines required real-world evidence files for iOS physical-device validation, Android real-browser validation, normal-browsing false-positive corpus validation, performance profiling, store/ad sandbox testing, and deployed AI backend smoke testing. `docs/validation/evidence-runbook.md` provides the field-by-field capture procedure, artifact expectations, thresholds, and final command sequence for those evidence files. `docs/validation/templates/` contains copyable JSON templates, `scripts/validation-evidence-specs.json` stores the shared evidence spec, `npm run evidence:requirements` prints that schema-versioned spec plus `generatedAt`, `productionEnvChecklist`, `handoffDocuments`, `handoffDocumentCommands`, and `productionBlockerGroups` as a machine-readable QA handoff, `npm run evidence:scaffold` creates safe draft packages with a generated `CAPTURE_PLAN.md` checklist under `docs/validation/artifacts/` without writing placeholders into `docs/validation/evidence/`, `npm run evidence:ios-physical-device -- --device <udid-or-name> --adult-host <real-adult-host> --app <signed-freed-app-or-ipa> --short-form-url <youtube-shorts-url> --run-id <run-id> --output-dir docs/validation/artifacts/<run-id>/ios-physical-device-capture` collects physical iPhone metadata and a manual recording checklist after rejecting placeholder/local/insecure normal/adult targets, `npm run evidence:android-real-browser -- --device <serial> --adult-url <real-adult-url> --permission-proof --native-status-proof --dns-guard-proof --run-id <run-id> --output-dir docs/validation/artifacts/<run-id>/android-real-browser-capture` collects physical Android browser screenshots/logcat/top-activity artifacts for manual review after rejecting placeholder/local/insecure URLs, and fails blank/malformed PNG captures before writing the per-attempt nonblank PNG render audit into the manifest, `npm run evidence:normal-browsing-corpus -- --run-id <run-id>` creates `normal-browsing-corpus-matrix.csv` plus `normal-browsing-browser-checklist.md` for iOS Safari plus Android Chrome, Firefox, Edge, and Samsung Internet from the release template's real external URL sets after rejecting placeholder/local/insecure manual browser targets, and exports the shared classifier corpus separately to `classifier-corpus-static.csv` as static classifier proof instead of physical browser QA rows, `npm run evidence:performance-profile -- --ios-device <udid-or-name> --android-device <serial>` creates a pending manual-QA profiling matrix, safe device metadata, and `performance-profile-evidence-fill-template.json` after rejecting placeholder/local/insecure normal browsing URLs, `npm run evidence:store-ad-sandbox -- --release-env-file <production-env-file>` creates a sanitized purchase/restore/rewarded-ad/privacy-review matrix, `npm run evidence:ai-backend-smoke -- --release-env-file <production-env-file>` creates a sanitized deployed AI eval/smoke/redaction/fallback matrix, `npm run evidence:validation:draft -- docs/validation/artifacts/<run-id>/draft-evidence` runs the same validator against a draft package before promotion, `npm run evidence:promote -- --from docs/validation/artifacts/<run-id>/draft-evidence` validates every draft and refuses partial copies before promotion, and `npm run evidence:templates` verifies templates, checked-in draft package JSON, `requirements.json`, `CAPTURE_PLAN.md`, plus README/runbook handoff commands stay aligned with the shared spec and command source. The template audit now also checks iOS physical-device, Android real-browser, store/ad sandbox, AI backend smoke, performance, and normal-browsing field examples, including normal-browsing corpus counts, URL groups, browser-matrix counts, required Android Firefox/Edge/Samsung Internet rows, required Google/YouTube/Instagram/X/education/streaming/gaming/productivity/recovery/adult-intent examples, required physical challenge-verification proof fields for fresh camera labels, no base64/EXIF photo payload, temporary-photo cleanup, motion samples, steps, and accurate foreground location fixes on both iOS and Android, the generated production env checklist, and shared capture/report command drift in the public handoff docs. `npm run evidence:validation` validates only real evidence files in `docs/validation/evidence/` and fails until real evidence is captured there. Evidence entries must point to an existing non-empty file under `docs/validation/artifacts/` or an HTTPS QA/report URL that passes the same non-local, non-private, non-placeholder host checks as production AI endpoints; remote evidence URLs must also look like QA/report/artifact paths rather than generic public webpages. The validator rejects copied template placeholder values, angle-bracket placeholders such as `<production-env-file>`, template env files such as `.env.production.example`, and remote/flag-shaped env-file proof inside submitted evidence fields, so placeholder strings cannot satisfy the gate; iOS evidence must include explicit physical-device identity, Family Controls entitlement team ID/artifact, app-group provisioning profile ID/artifact, Family Controls status, selected token counts, concrete Screen Time/picker/filter/shield/DeviceActivity/challenge-verification/normal-browsing/adult-intercept run IDs, per-flow artifact references, adult-intercept proof, Safari web short-form block proof, and Vision camera-label/motion/step/location challenge verification thresholds; Android evidence must include explicit physical-device identity, Accessibility, Chrome/Firefox/Edge/Samsung Internet package proof, concrete browser/WebView/DNS/back-stack/challenge-verification/normal-browsing run IDs and artifact references, native handoff, adult-intercept proof, ML Kit camera-label/motion/step/location challenge verification thresholds, Accessibility Play policy review ID/artifact, and special-use foreground-service policy review ID/artifact; normal-browsing evidence must include the shared classifier safety corpus source/count/pass/fail summary, a physical-device browser matrix covering iOS Safari plus Android Chrome, Firefox, Edge, and Samsung Internet, per-browser run IDs, result artifacts, exact allowed/recovery/adult pass counts, zero false positives, zero missed adult blocks, Google, YouTube, Instagram, X/Twitter, education, streaming, gaming, productivity, recovery-search allow cases, and adult-search block cases; performance evidence must meet concrete aggregate and per-platform iOS/Android physical-device battery/RAM/thermal/DNS/speed/background CPU thresholds, with run IDs, profiler artifacts, background CPU artifacts, network-speed/DNS-latency run IDs plus artifacts, no-polling proof, and no-full-traffic-proxy proof in each platform row; store/ad evidence must include a production-safe purchase verification endpoint, sanctioned `npm run preflight:release-env` command proof with run ID/artifact, sanctioned `npm run smoke:purchase-verification` command proof with optional `-- --env-file`, platform product IDs, concrete iOS purchase/restore and Android purchase/restore run IDs plus artifacts, sanitized purchase, restore, entitlement, purchase/restore verification report IDs, artifacts, and pass/failure counts, real-format AdMob rewarded-ad unit, rewarded request artifact proving load/non-personalized mode/coarse country context, App Store/Play privacy-disclosure review ID/artifact, free streak-risk rewarded intervention proof with latency at 5000 ms or less, rewarded completion/fallback run IDs and artifacts, premium no-ad intervention run ID/artifact with latency at 3000 ms or less, and rewarded-ad response identifiers without raw Android purchase tokens, raw receipts, private customer identifiers, ad-network secrets, or store-verification credentials; AI backend evidence must include production-safe coach/challenge endpoints, sanctioned `npm run preflight:release-env` command proof with run ID/artifact, sanctioned `npm run eval:ai-safety` and `npm run smoke:ai-backend` command proof with optional `-- --env-file`, safety-eval and smoke report IDs/artifacts, coach/challenge smoke run IDs/artifacts, challenge personalization run ID/artifact with at least two profile shapes, aggregate urge-risk forecast proof, and zero premium-only free challenges, no-sensitive-echo sample count plus run ID/artifact, redaction report ID/artifact, crisis fallback run ID/artifact, and provider fallback run ID/artifact, without raw prompts, private notes, transcripts, unredacted responses, sensitive URLs/domains, or provider API keys. | Partial |
| Release readiness audit | `npm run audit:release` checks actual prototype/design files, Expo app surface, prompt-to-artifact traceability, release verifier command sequencing, release environment preflight harness presence, production env template completeness, EAS/store-launch config with manual internal/store artifact workflows, Core 3-only product metadata, `store/console-launch-packet.md`, `store/play-store/data-safety.md`, `store/app-store/app-privacy.md`, draft-gated store submission setup, and hosted privacy/support/account-deletion URL availability, validation evidence requirements/scaffold/draft/promote workflow wiring, adult-only classifier behavior against the shared classifier safety corpus plus Android classifier parity, iOS Screen Time scaffold, Android no-overlay/DNS-only native safety contracts, challenge verification contract, challenge personalization context, discipline configuration contract, accessibility safety contract, privacy safety contract, runtime data integrity contract, backend architecture contract, production backend infrastructure readiness, production analytics ingestion endpoint readiness, production notification backend readiness, production adult-domain feed endpoint/source readiness, Android upload signing readiness, production dependency audit, monetization adapter scaffold, server AI and retention route presence, AI safety eval harness presence, backend readiness smoke harness presence, Supabase schema smoke harness presence, adult-domain feed smoke harness presence, analytics ingestion smoke harness presence, remote notification smoke harness presence, AI backend smoke harness presence, purchase verification smoke harness presence, client bundle secret audit harness presence, production monetization readiness, remote AI readiness, and concrete validation evidence files for physical-device, normal-browsing, performance, store/ad, and AI backend smoke gates. The runtime data gate now also proves production builds force native monetization when a mock flag is present, local urge forecasts use real local signals without random/mock scoring, source-aware intervention copy distinguishes app/short-form shields from adult web/search blocks, and challenge copy avoids impossible-to-fake verification claims. The validation workflow now also guards capture-helper `--output-dir` paths so pending manifests and fill templates can only be written under `docs/validation/artifacts/<run-id>/`, matching the validator's accepted local artifact root, and guards production env-file paths so secrets cannot be read from validation evidence/artifact folders. The production readiness gates now independently reject missing Android upload signing env/files, unavailable hosted store legal URLs, and invalid optional timeout and response-size env values for purchase verification, adult-feed sync/source fetches, aggregate analytics, Supabase/schema/hosted-backup sync, remote notifications, and remote AI, so `audit:release` cannot rely solely on the separate preflight command for transport bounds. `npm run audit:release -- --report docs/validation/artifacts/<run-id>/release-readiness-report.json` writes a sanitized `release-readiness-report-v1` summary, and `npm run verify:release` starts with `npm run preflight:release-env`, supports `--env-file <path>` with env-file values overriding stale shell variables for every child command, supports safe `--artifact-dir docs/validation/artifacts/<run-id>` report collection for release preflight, hosted legal URL availability, upload-signed Android APK build, deployed backend/Supabase/adult-feed/analytics/notification/AI/purchase smoke gates, and the final strict release-readiness audit, validates those report artifacts with executable smoke-harness self-tests, preserves app-level env-file flags through `node --`, rejects unsafe `--env-file`, `FREED_RELEASE_ENV_FILE`, preflight/smoke/release-readiness `--report`, and release artifact-directory values before loading, then runs typecheck, core tests, smoke harness self-tests, web export, hosted legal URL audit, Android bundle export, upload-signed Android APK build, client bundle secret audit, backend readiness smoke, Supabase schema smoke, adult-domain feed smoke, analytics ingestion smoke, remote-notification smoke, classifier/native-parity/accessibility/privacy/runtime-data/backend/performance/challenge audits, and the final fail-closed command sequence once the production preflight passes. Current result: `npm run audit:release` is 30 pass, 0 warn, 14 fail without local signing env and 31 pass, 0 warn, 13 fail with `~/.freed/android-upload/freed-upload.env` loaded; `npm run verify:release -- --list` includes `npm run audit:smoke-harnesses` immediately after `npm run test:core`, `npm run audit:store-legal-hosted` after web export, `npm run build:android-apk:upload-signed` after Android bundle export, `npm run smoke:backend-readiness`, `npm run smoke:supabase-schema`, `npm run smoke:adult-domain-feed`, `npm run smoke:analytics-ingestion`, and `npm run smoke:remote-notifications` after client bundle secret audit, and `npm run audit:runtime-data` plus `npm run audit:backend` before performance/dependency audits; `npm run preflight:release-env` correctly fails closed, currently 9 pass, 22 fail from the unsigned shell and 10 pass, 21 fail with the Android signing env loaded. The failures are the known production gates for hosted legal URL availability, missing native IAP/AdMob production env vars, purchase-verification endpoint and server-only App Store / Google Play verification credentials, Supabase/Redis backend secrets, deployed analytics/backend-readiness endpoints, FCM/APNs notification backend credentials, deployed adult-domain feed endpoint/source configuration, remote AI endpoint configuration, missing iOS physical-device evidence, missing Android real-browser evidence, missing normal-browsing corpus evidence, missing performance evidence, missing store/ad sandbox evidence, and missing AI backend smoke evidence. | Partial |
| TypeScript verification | `npm run typecheck` passes. | Done |
| Web smoke verification | Browser/Playwright smoke tests were run against Expo web and static export for free flow, premium flow, normal allow, adult search block, persisted onboarding/blocked attempts, persisted daily check-ins, reminder web fallback behavior, web no-op notification listener loading, Profile Native Protection fallback copy, and Analytics weekly report rendering. Latest static-export DevTools smoke rendered FREED in clean headless Chrome, clicked `Skip to Dashboard`, selected the Analytics tab by accessibility label, and confirmed the `PRIVACY-SAFE SNAPSHOT` card renders with aggregate-only copy and local sharing status. Screenshots saved in `/tmp` for earlier visual passes. | Done for web prototype |
| Static export | `npm run export:web` produced `dist/` after the monetization configuration, native-handoff freshness, CLARA remote-provider/crisis/server route, dynamic challenge generator/server route, purchase verification route/server-token changes, search-engine classifier, adaptive analytics signals, XP/achievement UI, private slip-log UI changes, and aggregate-only analytics snapshot card. A targeted bundle audit found no App Store private-key/JWT or Google Play service-account/access-token secret names in the static web or Android embedded outputs. | Done |
| iOS simulator run | `npx expo run:ios --device "iPhone 17 Pro Max" --no-bundler` built, installed, and opened `app.freed.recovery` on the booted iPhone 17 Pro Max simulator after adding `ExpoSQLite` and `ExpoNotifications`. `FreedProtection`, `ExpoSQLite`, and `ExpoNotifications` compiled and packaged. After adding Screen Time extensions, `xcodebuild -workspace ios/FREED.xcworkspace -scheme FREED -configuration Debug -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' CODE_SIGNING_ALLOWED=NO COMPILER_INDEX_STORE_ENABLE=NO build` passed. After adding the native monetization SDKs, `pod install --repo-update` resolved the iOS pods and the same simulator `xcodebuild` passed again. After adding `expo-location` and `expo-sensors` for real challenge verification, `pod install` resolved `ExpoLocation 55.1.10` and `ExpoSensors 55.0.15`, and simulator `xcodebuild` passed. After adding camera/photo verification, `pod install` resolved `ExpoImagePicker 55.0.20`, and `xcodebuild -workspace ios/FREED.xcworkspace -scheme FREED -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max,OS=26.2' -derivedDataPath ios/build CODE_SIGNING_ALLOWED=NO build` passed with `** BUILD SUCCEEDED **`. Xcode validated the embedded `FREEDShieldAction.appex`, `FREEDShieldConfiguration.appex`, and `FREEDDeviceActivityMonitor.appex` bundles. The Debug target now provides Google's simulator-safe test `GADApplicationIdentifier` while Release stays env-driven, which fixed the prior Google Mobile Ads launch abort. The current local runtime smoke installed the Debug app on iPhone 17 Pro Max, confirmed the expected no-Metro `No script URL provided` redbox because Debug sets `SKIP_BUNDLING=1`, started Metro on `localhost:8081`, observed the cold bundle complete in `700823ms`, then relaunched against warm Metro and rendered the FREED home UI. The repeatable `npm run smoke:ios-simulator` helper now warms the bundle before launch, captures a rendered home-screen artifact, audits the PNG for dimensions, visible pixels, color variety, and luminance variance, scans recent simulator logs for app-specific crash/redbox signals, writes a metadata JSON sidecar by default, and stops Metro; `node scripts/ios-simulator-smoke.js --self-test` covers clean launch, redbox, native exception, and unrelated crash parser cases, while `node scripts/lib/png-screenshot-audit.js --self-test` covers useful, solid, tiny, and malformed PNG cases. After adding iOS Complete Data Protection entitlements, entitlement plists and automated JS/privacy gates passed; the native build helper now auto-selects an available simulator through bounded `xcrun simctl` before invoking xcodebuild and prints an explicit dry-run placeholder note when `auto` is intentionally unresolved. The current auto native rebuild passed with `npm run build:ios-simulator -- --simctl-timeout-ms 15000 --log docs/validation/artifacts/local-simulator-smoke/ios-native-build-20260524-auto.log`; it selected `iPhone 17 Pro Max` (`4E748628-BD69-48F2-B02B-9A6402FC8389`), built Debug with signing disabled into `ios/build`, validated `FREEDShieldConfiguration.appex`, `FREEDShieldAction.appex`, `FREEDSafariContentBlocker.appex`, and `FREEDDeviceActivityMonitor.appex`, and ended with `** BUILD SUCCEEDED **` after 523162 ms. | Local simulator rebuild complete; entitlement-approved physical-device evidence still required |
| Android emulator run | `./gradlew :app:assembleDebug` passed. After the monetization configuration, native-handoff, CLARA remote-provider/crisis, dynamic challenge generator, search-engine classifier, and adaptive analytics signal changes, `CI=1 EXPO_NO_TELEMETRY=1 npx expo export:embed --platform android --entry-file node_modules/expo-router/entry.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res` regenerated the Android bundle and `./gradlew :app:assembleDebug` passed. After adding `react-native-purchases`, `react-native-google-mobile-ads`, `expo-location`, `expo-sensors`, `expo-image-picker`, and ML Kit image labeling, debug builds continued to pass and produced `android/app/build/outputs/apk/debug/app-debug.apk`. The release bundle export now includes `--dev false --minify true`, `./gradlew :app:assembleRelease -PreactNativeArchitectures=arm64-v8a` produced `android/app/build/outputs/apk/release/app-release.apk`, and `npm run smoke:android-emulator -- --device emulator-5554 --apk android/app/build/outputs/apk/release/app-release.apk --install-fixture --clear-app-data --output docs/validation/artifacts/local-android-emulator-smoke/android-smoke-rendered-release-default-avd.png --launch-wait-ms 60000` installed the release APK plus QA fixture on `FREED_Default_API35` (Android 15), launched `app.freed.recovery/.MainActivity` cold in 1079 ms, and captured a rendered FREED onboarding screen. Filtered logcat showed `ReactNativeJS: Running "main"` and `Displayed app.freed.recovery/.MainActivity ... +1s79ms` with no matching app crash/ANR lines; `scripts/android-emulator-smoke.js` now clears/scans logcat automatically, audits the PNG for dimensions, visible pixels, color variety, and luminance variance, fails future smoke runs on package-specific crash/ANR signals or blank screenshots, and writes a metadata JSON sidecar by default. Debug APK smoke attempts documented earlier issues: debug builds expect Metro, the embedded bundle must be production mode, streamed installs can hang on large APKs, and the Expo Notifications `MY_PACKAGE_REPLACED` receiver needed a manifest override to avoid background startup during install. The default AVD lacks Google Play Store, so store/ad sandbox validation still requires a Play image or physical device. | Done for local debug/release emulator launch; real-device browser/store/performance evidence missing |
| Real adult-content redirects from browser | Simulated in app and partially bridged on Android through AccessibilityService pending interventions. Native pending interventions are consumed on launch/foreground with native-plus-JS stale-record protection, clearing missing, future-dated, or older-than-ten-minute handoffs before recovery UI opens, and converted into blocked attempts without reclassifying host-redacted URLs, so native adult-search verdicts survive privacy redaction. iOS shield handoffs are likewise consumed only after stale-record cleanup and host-only pending value normalization. iOS shield extension targets and the Safari Content Blocker now compile and embed, but real shield actions, content-blocker feed reload/adult blocking, device activity schedules, app handoff behavior, and browser validation still need Family Controls provisioning and physical-device testing. | Partial |
| Rewarded ads | Mock rewarded reset and no-ad fallback are implemented through `src/lib/monetization.ts` so free-user recovery is never blocked when an ad provider is unavailable. Native-mode configuration now checks for platform-specific AdMob app IDs, rewarded reset ad units, and optional coarse `EXPO_PUBLIC_ADMOB_REQUEST_COUNTRY` metadata for release QA/ad-mediation evidence. `react-native-google-mobile-ads` is installed, `app.config.js` conditionally wires its Expo config plugin from platform env vars, and `src/lib/native-monetization-runtime.ts` creates AdMob rewarded sessions with non-personalized ad requests while preserving the recovery fallback. Live inventory and sandbox completion validation are not complete yet. | Partial |
| False-positive suite beyond classifier | Core tests, `npm run audit:classifier`, and `npm run audit:android-classifier` cover the normal-domain allowlist, known search-engine allow behavior, a broader false-positive corpus across search/social/education/dev/streaming/gaming sites, recovery/education searches that mention adult terms, overmatch guards for adult-looking hostname fragments, recovery/filtering hostnames with adult-looking tokens, and mixed consumption wording or consumption hostnames that should still block. The Android classifier mirror compiles with the same recovery-context hostname logic. Broad real browsing suite not run on devices. | Partial |
| Battery/internet performance tests | Static implementation safety audit passes via `npm run audit:performance`, but physical/emulator battery, thermal, RAM, and network-speed profiling has not been run. | Partial |

## Next Required Work

Recent backend hardening note: `src/lib/backend-infrastructure.ts` now provides server-only hashed Redis rate limits that fail closed in production when Redis is unavailable, adult-feed sync locks, and sanitized `backend_job_runs` persistence; `src/lib/backend-provider-timeout.ts` keeps shared Redis/Upstash and Supabase backend provider calls bounded with `FREED_BACKEND_PROVIDER_TIMEOUT_MS` and `FREED_BACKEND_PROVIDER_RESPONSE_MAX_BYTES`; CLARA, challenge, retention, purchase verification, analytics ingestion, notification dispatch, encrypted backup sync, maintenance cleanup, and adult-feed routes call the rate-limit guard or backend job runner, and adult-feed publication records lock/job-run status without storing raw URLs/domains or client identifiers. `src/lib/server-request-body.ts` bounds public JSON POST bodies and rejects non-JSON, malformed, and oversized payloads before provider calls, while `src/lib/bounded-response-json.ts` bounds provider JSON response reads and rejects response-like objects that cannot expose a readable byte-checkable body. `src/lib/server-ai-provider.ts` now bounds OpenAI/Gemini provider requests and JSON reads so CLARA, challenge, and retention routes fall back instead of hanging. Native purchase verification, aggregate analytics sharing, hosted encrypted-backup sync, Supabase Auth, remote notifications, purchase providers, AI providers, and shared backend Supabase/Redis providers now all use timeout- and response-size-bounded JSON reads. `src/lib/remote-notifications.ts` plus `app/api/notifications/send+api.ts` add a server-authorized, timeout- and response-size-bounded FCM/APNs push boundary that accepts only platform, device token, and a preset recovery-safe kind, then sends prewritten copy with route/kind metadata only. `src/lib/recovery-backup-sync.ts` plus `app/api/recovery-backup/sync+api.ts` add an authenticated hosted-continuity boundary for encrypted backup envelopes only. `src/lib/backend-retention-cleanup.ts` plus `app/api/backend/cleanup+api.ts` add secret-authorized cleanup for expired backend rows without selecting stored payloads.

Recent evidence hardening note: `npm run smoke:backend-readiness`, `npm run smoke:supabase-schema`, `npm run smoke:adult-domain-feed`, `npm run smoke:analytics-ingestion`, `npm run smoke:remote-notifications`, `npm run smoke:purchase-verification`, and `npm run smoke:ai-backend` now support `--report docs/validation/artifacts/<run-id>/...json` so production QA can attach sanitized `backend-readiness-smoke-v1`, `supabase-schema-smoke-v1`, `adult-domain-feed-smoke-v1`, `analytics-ingestion-smoke-v1`, `remote-notification-smoke-v1`, `purchase-verification-smoke-v1`, and `ai-backend-smoke-v1` artifacts instead of copying console output. These report paths are rejected by a shared helper if they point directly into `docs/validation/evidence/`, including case variants on case-insensitive filesystems, keeping generated reports as supporting artifacts that must be referenced by completed evidence JSON. These reports keep only readiness booleans, missing env-key names, Supabase schema service-role/public-lockout/limit=0 boundary proof, adult-feed version/checksum/count/cache/source metadata, aggregate analytics counts, notification dispatch-boundary proof, synthetic purchase-verification rejection proof, AI endpoint/model/redaction boundary proof, pass/fail status, endpoint path proof, and redacted detail strings; they do not store server secrets, Supabase service-role keys, anon JWTs, row payloads, full adult-domain lists, reviewed source URLs with query strings, analytics snapshots, raw rejection payloads, push tokens, dispatch secrets, store credentials, raw store receipts, purchase tokens, order IDs, raw prompts, private notes, unredacted model responses, coordinates, custom notification copy, browsing URLs, contacts, receipts, or tokens.

Recent Android DNS Guard hardening note: `modules/freed-protection/android/src/main/java/app/freed/protection/FreedVpnService.kt` now parses the IPv4/UDP DNS envelope separately from the DNS question host, counts malformed questions, and returns bounded SERVFAIL responses when the envelope is available. This keeps the DNS-only VPN fallback low-latency by avoiding silent client hangs without adding packet inspection, full-traffic proxying, or full-device routing.

Recent blocked-attempt privacy hardening note: `src/lib/recovery-state.ts` now hydrates legacy attempts through an explicit safe field allowlist instead of spreading stored objects back into state. It redacts the stored URL/host, clamps confidence, sanitizes reason and matched-rule text, normalizes app source packages, and drops top-level or nested raw URL/note fields before local state can be persisted again.

See `docs/release-blockers.md` for the concrete env vars, evidence files, and commands tied to the current production credential, endpoint, and evidence release gates. A generated QA handoff package for the remaining evidence captures now lives at `docs/validation/artifacts/2026-05-13-release-evidence/CAPTURE_PLAN.md`; its draft JSON files are placeholders for capture only and must be filled, draft-validated, and promoted before they count as release evidence.

1. Finish production iOS Screen Time work: Apple-approved Family Controls/app-group provisioning, shield action handoff validation, Safari Content Blocker signed-target/reload/adult-block validation, and physical-device validation of the picker, ManagedSettings filter, selected app/domain shields, selected-target daily-limit threshold, and Night Guard DeviceActivity schedule, then capture `docs/validation/evidence/ios-physical-device.json`.
2. Finish Android production interception: real-device validation across supported browsers, Usage Access aggregate timer diagnostics, synced adult-domain feed classification in Accessibility and DNS Guard, native handoff/back-stack hardening, DNS/private-DNS guidance, Play policy review for DNS guard foreground-service subtype, long-running DNS Guard lifecycle validation using the native session counters including malformed/SERVFAIL behavior, and reboot/package-update restart validation using the native restart diagnostics, then capture `docs/validation/evidence/android-real-browser.json`.
3. Configure real App Store / Play products, purchase-verification endpoint, AdMob app ID, rewarded-unit env vars, optional coarse country metadata, and optional RevenueCat fallback only if selected; run `npm run evidence:store-ad-sandbox -- --release-env-file <production-env-file>` to scaffold the sanitized sandbox matrix with non-secret release-env context prefilled, explicit CLI flags overriding env-file values, and placeholder/local/private/insecure purchase-verification endpoints plus URL credentials, query strings, and fragments rejected when provided; validate iOS purchase/restore, Android purchase/restore, purchase/restore verification reports, entitlement, non-personalized ad request proof, country context proof, App Store/Play privacy-disclosure review proof, free streak-risk rewarded intervention, rewarded-ad completion, ad-failure fallback, and premium no-ad intervention in sandboxes with run IDs, artifact references, `freePostAdChallengeLatencyMs<=5000`, and `premiumNoAdLatencyMs<=3000` while preserving the no-ad recovery fallback; then capture `docs/validation/evidence/store-ad-sandbox.json`.
4. Wire CLARA's remote provider boundary and the dynamic challenge generator boundary to a production privacy-reviewed AI backend, run `npm run evidence:ai-backend-smoke -- --release-env-file <production-env-file>` to scaffold the sanitized smoke matrix with non-secret release-env context prefilled, explicit CLI flags overriding env-file values, and placeholder/local/private/insecure AI endpoints plus URL credentials, query strings, and fragments rejected when provided, then run `npm run eval:ai-safety` and `npm run smoke:ai-backend` against the release build and add live model monitoring/evaluation evidence, including challenge-personalization profile proof.
5. Add broad false-positive browsing tests and real adult-domain interception tests on iOS/Android.
6. Run `npm run evidence:performance-profile -- --ios-device <udid-or-name> --android-device <serial>` to scaffold the profiling capture, then complete battery, RAM, network-speed, DNS-latency, routing-proof/no-full-traffic-proxy, and long-session stability profiling on physical devices before capturing `docs/validation/evidence/performance-profile.json`.
7. Keep hosted encrypted sync gated behind production Supabase setup until privacy disclosure review, Supabase Auth redirect testing, and production evidence prove the route stores encrypted envelopes only.
8. Expand accountability from local SMS/email and aggregate sponsor reports into remote community workflows only after the `src/lib/community-safety.ts` consent, privacy-disclosure, guidelines, moderation queue, abuse-reporting SLA, all-surface block/mute, retention/deletion, disabled DM/user-text, crisis-review, and aggregate-only readiness gate is satisfied by product/legal/release review.
