# FREED Prompt-To-Artifact Checklist

Objective: build a world-class porn recovery and adult-content blocking mobile app for iOS and Android by inspecting the provided prototype/design files, researching the required references, implementing the app, and verifying it in this workspace.

Current conclusion: not complete. The local app, native scaffolds, safety audits, simulator/emulator builds, and validation harnesses exist, but the release still lacks production credentials/endpoints and real device/store/performance evidence.

## Source Inputs

| Prompt requirement | Concrete evidence | Current state |
| --- | --- | --- |
| First check the existing prototype/design/name files | `freed.html`, `ios-frame.jsx`, `freed-v2/screens-onboarding.jsx`, `freed-v2/screens-home.jsx`, `freed-v2/screens-main.jsx`, `freed-v2/tokens.css`, `freed-v2/tokens.js`, `ref/*.png`, `ref2/*.png`; `npm run audit:release` gate `prototype-design-files` checks the required file set. | Present; app name is FREED. |
| Study mandatory Google Play reference app | Listed in `docs/research-and-architecture.md` as `https://play.google.com/store/apps/details?id=io.funswitch.blocker`, with a 2026-05-13 refresh of listing position, permissions, monetization/trust risks, and normal-browsing product lessons. | Researched at product-architecture level; refresh again before launch copy/ASO. |
| Study mandatory Mobbin QUITTR flow | Listed in `docs/research-and-architecture.md`; local visual references in `ref/` and `ref2/` are used because unauthenticated Mobbin access remains limited as of 2026-05-13. | Partial because live Mobbin has access limits; local exported screens are the concrete visual source. |
| Research blocker competitors, addiction recovery, dopamine interruption, habits, streaks, behavioral design, platform limits, premium wellness UX | Source list and synthesis live in `docs/research-and-architecture.md`. | Done for architecture; should be refreshed before final launch positioning. |

## Product Scope

| Prompt requirement | Concrete evidence | Current state |
| --- | --- | --- |
| Build a recovery product, not just a blocker | `src/features/freed-app.tsx`, `src/lib/recovery-engine.ts`, `src/lib/recovery-state.ts`, `src/lib/challenge-generator.ts`, `src/lib/ai-coach.ts`; completion audit rows for challenge, CLARA, retention, analytics. | Implemented for prototype/local-first release. |
| Explain permissions before activation | `AppSelectionScreen` and `ProtectionSetupScreen` in `src/features/freed-app.tsx`, `src/lib/protection-permissions.ts`, `src/lib/protection-readiness.ts`, `src/lib/recovery-state.ts`, `npm run audit:privacy` gate `permission-flow-disclosure-boundaries`, and core tests for app-selection ordering, persisted paywall/setup resumption, iOS/Android permission-plan ordering, data boundaries, native setup gating, stale activation clearing, aggregate iOS Screen Time target-count readiness, and the no-history Test Protection activation check. | Implemented locally with recovery goals -> app selection -> paywall -> permission setup ordering. The setup screen now uses a shared production explanation that FREED needs permissions to monitor only selected apps/sites through platform APIs, block known adult domains, and open recovery challenges when a harmful site, search, or app-limit threshold is reached. Onboarding completion, paywall presentation, and platform-scoped protection activation are persisted separately, so a restart mid-flow resumes the paywall/setup path instead of bypassing activation. If required iOS/Android native setup readiness is later revoked, including adult filtering, app-limit scheduling, or configured app-timer readiness, FREED clears the saved activation and returns dashboard-style screens to setup instead of treating stale activation as proof. iOS setup readiness, permission rows, and Profile diagnostics use the shared selected Screen Time target counter, so either the aggregate `selectedScreenTimeTokenCount` from the native bridge or individual app/category/domain counts can prove selected target readiness without exposing token identities. Android activation now includes native adult-domain feed sync/count readiness in addition to DNS Guard, Usage Access, Accessibility, and selected app timers; the setup CTA only becomes Activation Complete after the activation test passes against a freshly refreshed native readiness sample, and iOS/Android setup does not continue into the main app until activation is complete. Release evidence now requires physical-device permission wizard proof with the exact `onboarding-goals>app-selection>paywall>protection-explanation>permission-setup>test-protection>activation-complete` order, a `permissionExplanationSummary` carrying the production selected-app/site, adult-domain, and harmful site/search/app-limit threshold copy, and Test Protection passing. Physical-device permission walkthrough evidence is still required. |
| Only block adult content and allow normal browsing | `src/lib/blocking-engine.ts`, Android mirror `modules/freed-protection/android/src/main/java/app/freed/protection/FreedUrlClassifier.kt`, broad classifier tests in `tests/core.test.ts`, `scripts/classifier-safety-corpus.ts`, `npm run audit:classifier`, `npm run audit:android-classifier`, and release gate `adult-only-classifier`. | Implemented for app-side and Android native classifier; still needs broad real-device browsing evidence. |
| Do not break internet speed, battery, normal browsing, or device experience | Android DNS-only fallback with resolver failover, bounded SERVFAIL outage handling, VPN revocation cleanup, and Private DNS diagnostics; iOS Screen Time/Safari protection with no NetworkExtension path; no overlay permission; event-driven app shields; runtime no screenshot/OCR/screen-capture or continuous image classification; and per-platform performance/routing evidence. | Static safeguards pass; physical performance evidence is missing. |
| Premium, emotional, clean, smooth, supportive UX matching design language | `src/constants/design.ts`, `src/features/freed-app.tsx`, local visual references in `ref/` and `ref2/`, prior web/mobile smoke screenshots noted in `docs/completion-audit.md`, and `npm run audit:accessibility` for key named controls, tabs, radio choices, and urgent-support action semantics. | Done for prototype; final store build still needs device UX QA. |

## Blocking And Native Platforms

| Prompt requirement | Concrete evidence | Current state |
| --- | --- | --- |
| Smart adult-only blocking engine | `src/lib/blocking-engine.ts`, `src/lib/adult-domain-feed-ingestion.ts`, `src/lib/adult-domain-feed-sync.ts`, `src/lib/adult-domain-feed-publication.ts`, `modules/freed-protection/android/src/main/java/app/freed/protection/FreedUrlClassifier.kt`, `app/api/adult-domain-feed+api.ts`, `tests/core.test.ts`, `npm run audit:classifier`, `npm run audit:android-classifier`, release gate `adult-only-classifier`. | Done for classifier, normalized feed scaffold, server-side reviewed source ingestion with bounded cache/in-flight reuse, timeout- and byte-bounded source refresh and stale-if-error protection, centralized timeout- and byte-bounded device feed JSON reads, timeout-bounded Supabase feed-version publication metadata, checksum/ETag/cache/source-size-header publication, app-side conditional checksum/304 sync, unchanged native-feed skip behavior, and Safari rule export; production source review ops and device evidence still missing. |
| iOS Screen Time, FamilyControls, DeviceActivity, ManagedSettings, Safari/web protection | `modules/freed-protection/ios/FreedProtectionModule.swift`, `ios/FREEDShieldConfiguration`, `ios/FREEDShieldAction`, `ios/FREEDDeviceActivityMonitor`, `ios/FREEDSafariContentBlocker`, `ios/FREEDSafariFocusShield`, `src/lib/adult-domain-feed-sync.ts`, and `docs/store-policy/ios-screen-time-safari-dns-review.md`; release gate `ios-screen-time-scaffold`. | Screen Time shields use atomic pending handoff and exact application/domain/category relock. Safari Content Blocker is adult-domain-only; the iOS 15.4+ MV3 Safari Focus Shield handles approved short-form routes through a background-worker native relay and Universal Link. No iOS NetworkExtension path is exposed or required. Full workspace build remains pending the recorded CocoaPods blocker and physical-device evidence. |
| Android Accessibility, Usage Access, browser/WebView detection, opt-in app shields, DNS filtering/DNS-only VPN fallback | `modules/freed-protection/android/src/main/java/app/freed/protection/FreedDoomscrollApps.kt`, `FreedAccessibilityService.kt`, `FreedProtectionModule.kt`, `FreedUsageAccessConfigActivity.kt`, `FreedAdultDomainFeed.kt`, `FreedInterventionActivity.kt`, `FreedVpnService.kt`, `FreedUrlClassifier.kt`, `src/lib/adult-domain-feed-sync.ts`, `android/qa-webview-fixture`, `docs/store-policy/android-accessibility-and-fgs-disclosure.md`; release gate `android-native-safety-contract`. | Debug build passes; focused-WebView QA fixture exists; native package, short-form rule, and local handoff host IDs are centralized in `FreedDoomscrollApps.kt` and consumed by both the AccessibilityService and native module; native adult-domain feed sync is wired into Accessibility and DNS Guard with conditional checksum requests so unchanged remote feeds keep the cached native list; supported browser/WebView extraction covers stable and beta/dev/canary Chrome/Edge plus stable/beta Samsung Internet and now classifies raw focused address/search text before navigation only from known URL fields or focused URL/address/search-like controls, blocks high-confidence adult intent through a redacted local focused-search host, and allows recovery/education phrasing without storing the query; Android now tries the targeted FREED Accessibility service details route before broader Accessibility/app/system fallbacks, while Usage Access status/settings, Settings-displayed Usage Access reason/config activity, unique observed package names, aggregate selected-app same-day foreground minutes, and per-package minute maps are exposed for app timer QA; configured or discipline-mode-expanded doomscroll app packages maintain an event-driven same-day usage ledger, cross-check user-enabled UsageStats totals for restart-resilient daily limits, trigger native handoff only after the configured daily app limit is reached, wait for configurable sustained YouTube Shorts (`short-form:youtube-shorts`), Instagram Reels (`short-form:instagram-reels`), or TikTok For You (`short-form:tiktok-feed`) behavior before short-form handoff, and native sync now expands the single TikTok UI choice to the supported regional Android packages `com.zhiliaoapp.musically`, `com.ss.android.ugc.trill`, and `com.tiktok`; YouTube/Instagram/TikTok require selected/focused Shorts/Reels/For You surface evidence before using scroll cadence and again at the threshold deadline, the AccessibilityService descriptor subscribes to `typeViewScrolled`, and earned unlocks pause only the package that earned the active unlock; DNS Guard's local `VpnService` fallback routes only configured DNS resolver IPs, never proxies full traffic, and posts a no-overlay recovery notification with the redacted intervention handoff before attempting an immediate challenge activity launch so Android background-start restrictions still leave a visible challenge path; the Android Play policy pack now documents AccessibilityService opt-in/disclosure/data boundaries, DNS Guard special-use foreground-service justification, Data safety mapping, and required review evidence fields; real-browser/app-shield/short-form/feed-update evidence, long-session device evidence, and submitted Play policy review for AccessibilityService/special-use foreground service are missing. |
| Avoid heavy VPN/full traffic proxying and polling loops | `FreedVpnService.kt` routes only DNS resolver IPs, protects upstream sockets, fails over across the configured resolver routes, returns SERVFAIL when every upstream resolver fails so allowed DNS does not hang silently, cleans up when Android revokes VPN permission, and restores after reboot/package update only when the user previously enabled DNS Guard and Android VPN consent is still valid; `FreedProtectionModule.kt` exposes Private DNS mode/specifier plus last DNS Guard resolver/restart diagnostics; iOS DNS settings are entitlement-gated, matched-domain-only, and explicitly avoid packet-tunnel/full-VPN providers; `FreedAccessibilityService.kt` tracks app-shield usage from Accessibility foreground events and subscribed, surface-gated short-form scroll events without broad polling, and posts a no-overlay notification fallback before Activity handoffs so Android background-start restrictions still leave a recovery path; `npm run audit:performance` verifies the static contract, rejects runtime screenshot/OCR/screen-capture APIs, includes Safari web short-form path rules, and allows only on-demand challenge photo classification; `npm run evidence:performance-profile` now captures Android routing/proxy/Private-DNS/VPN state for manual DNS-only review, bounds `adb`/`xcrun` metadata commands with `--tool-timeout-ms`, and writes `performance-profile-evidence-fill-template.json` with false checks for final evidence handoff. | Static pass; device network-speed/background-CPU/routing proof missing. |

## Recovery Experience

| Prompt requirement | Concrete evidence | Current state |
| --- | --- | --- |
| Relapse interruption redirects into app with supportive recovery flow | App-side flow in `src/features/freed-app.tsx`; `src/lib/native-intervention.ts` converts fresh native pending interventions into first-class blocked attempts without reclassifying host-redacted URLs, and only carries allowlisted supported app packages into earned-unlock source metadata; Android pending native interventions and iOS Shield Action records are normalized to host-only URLs and consumed into the same React flow. | Prototype and native handoff scaffold done; physical browser/shield evidence missing. |
| Free user flow uses rewarded ad before challenge | `src/lib/monetization.ts`, `src/lib/native-monetization-adapter.ts`, tests for rewarded reset fallback and AdMob adapter; store/ad evidence requires `freeRewardedInterventionRunId`, `freeRewardedInterventionArtifact`, `freePostAdChallengeLatencyMs<=5000`, and checks proving streak-risk context, rewarded ad before challenge, and post-ad challenge generation. | Mock/native-ready; live free rewarded-ad sandbox evidence missing. |
| Premium user flow skips ads and starts intervention immediately | `src/features/freed-app.tsx`, `src/lib/monetization.ts`, core tests and store/ad evidence fields for `premiumNoAdInterventionRunId`, `premiumNoAdInterventionArtifact`, `premiumNoAdLatencyMs<=3000`, and `premiumNoAdInterventionStartsChallenge`. | Done for prototype; live premium entitlement no-ad evidence missing. |
| Challenge system is adaptive and learns what works | `src/lib/recovery-engine.ts`, `src/data/challenge-templates.ts`, `src/lib/challenge-generator.ts`, `src/lib/challenge-verification.ts`, challenge outcome/verification tests in `tests/core.test.ts`, release gate `challenge-personalization-context`, and AI backend evidence fields for `challengePersonalizationRunId`, `challengePersonalizationArtifact`, `challengePersonalizationProfileCount>=2`, `challengeRiskForecastProfileCount>=2`, `riskForecastPersonalizationVerified=true`, and `freeChallengePremiumCount=0`. The local template engine now assigns every generated challenge to production families (physical, outdoors, mindfulness, productivity, social, emergency, anti-relapse, late-night, quick-reset) while preserving the five simple UI categories; strict mode, sleep mode, outdoor/social preferences, deep focus, local urge forecast, and weather/location-permission context score against those families before any remote AI call. Premium custom resets and the legacy fallback challenge pool infer the same production families from category, title, why text, intensity, duration, and steps before local scoring. The local/remote request now uses time, day-part, weekend/timezone, preferences, slip summaries, outcome history, aggregate recent failed-reset counts, coarse intervention context including Android app/short-form session-duration buckets, local urge forecast, check-in energy/urge/sleep, real location-permission state, and coarse real-weather condition when available, without raw browsing details, coordinates, inferred weather/location, raw app usage logs, screen content, or user-authored custom reset text. Premium users can create local custom resets in the Library; those resets persist in `RecoveryState.customChallenges`, feed local selection, remain premium-only, and stay local even when remote challenge generation is configured. | Done locally plus remote-provider-ready boundary; live personalization smoke evidence missing. |
| Configurable limits, unlock duration, app shields, intensity, schedules, and modes | `src/lib/recovery-state.ts` persists `DisciplineSettings`, opt-in Android doomscroll app packages with user-facing primary choices plus native package aliases, short-form interruption threshold, and bounded `EarnedUnlock` history; `src/features/freed-app.tsx` exposes Shield-screen controls for daily limit, Shorts guard threshold, unlock duration, challenge intensity, outdoor/exercise/social frequency, emergency strict mode, sleep mode, editable sleep start/wake times, deep focus, work hours, editable work start/end times, weekend mode, and Android app shields; verified-completion earned unlocks are capped to `MAX_EARNED_UNLOCK_MINUTES=120` during recording/hydration and synchronized to `FreedProtection.applyEarnedUnlockWindow`/`clearEarnedUnlockWindow` with the redacted source host when available while challenge outcome feedback remains separate for personalization/support routing; iOS pauses selected app shields while preserving adult web filtering, normalizes Screen Time shield handoffs as opaque app/unknown interventions rather than browser/adult events, bounds native earned-unlock expiry to 120 minutes, syncs the configured sleep-mode window into minute-level `startRiskWindowMonitoring`, and Android syncs the bounded native expiry signal, inferred source app package, configured daily app budget, short-form threshold, and active app packages expanded to the full known doomscroll package set, including regional TikTok package aliases, during emergency strict, sleep, deep focus, work-hours, or weekend mode while preserving adult-domain filtering, allowing only the package that earned the unlock during the unlock window, clearing unsupported/source-less native unlock attempts, and scheduling a source-aware relock deadline after earned-unlock expiry; the JS surface uses the same source resolver to apply native Android unlocks only for supported app-package/short-form handoff sources and labels browser-sourced completions as challenge windows instead of app unlocks; `buildChallengePreferenceSignal`, `src/data/challenge-templates.ts`, and `src/lib/challenge-generator.ts` feed those settings into local/remote challenge selection; release gate `discipline-configuration-contract` and core tests cover hydration/clamping, Shield schedule controls, app-package sanitization and alias normalization, mode-expanded app-package sync, sleep-window schedule wiring, earned unlock expiry, daily-limit-aware native app-shield wiring, source-scoped Android earned unlocks, sustained short-form native bridge wiring, and no-raw-browsing preference signals. | Implemented locally and native-bridge wired; physical-device/store evidence still missing. |
| Real challenge verification | `src/lib/challenge-verification.ts` maps challenge content to timer, accelerometer motion, pedometer steps, foreground location movement, camera photo, or connection-action evidence; `src/features/freed-app.tsx` watches `expo-sensors`/`expo-location`, requires multiple accurate foreground location fixes before distance can satisfy outdoor movement challenges, opens fresh camera capture through `expo-image-picker` without base64/EXIF payloads, calls `FreedProtection.classifyChallengePhoto`, deletes the temporary camera file on a best-effort basis after classification, shows verification progress, keeps completion disabled until evidence satisfies the requirement, and lets connection challenges record an explicit connection action even when no accountability partner is configured. iOS uses Vision `VNClassifyImageRequest`; Android uses ML Kit on-device image labeling. The release evidence specs now require physical-device proof for camera-label confidence, no base64/EXIF payloads, temporary-photo cleanup, motion samples, steps, and accurate foreground location fixes on both platforms before promotion. | Implemented for motion, steps, location accuracy, timer, connection, and photo-label flows; physical-device sensor/camera QA capture is still missing. |
| Streak, XP, recovery score, levels, badges, milestones, analytics | `src/lib/recovery-state.ts`, `src/features/freed-app.tsx`, core tests for reports, XP, levels, badges, milestones, AppState session tracking, and aggregate analytics. | Done for local prototype. |
| AI recovery coach with privacy-safe remote option | `src/lib/ai-coach.ts`, `src/lib/challenge-generator.ts`, `src/lib/remote-provider-timeout.ts`, `src/lib/server-ai-provider.ts`, `app/api/clara+api.ts`, `scripts/ai-safety-eval.ts`, `scripts/ai-backend-smoke.ts`, and AI evidence fields for release-env preflight command/run ID/artifact proof, safety eval counts/artifacts, endpoint smoke counts/run IDs/artifacts, concrete remote provider model ID, challenge personalization profile proof, aggregate urge-risk forecast profile proof, no-sensitive-echo samples/run ID/artifact, redaction artifact, crisis fallback artifact, and provider fallback artifact. The client remote CLARA/challenge endpoints now reject unsafe local/insecure/private endpoints in readiness and use hard timeouts for both request and JSON parsing before falling back locally. The smoke script reports provider-aware `configured-ai-model` and `challenge-personalization-profiles`, covers context-rich weather/location-permission plus aggregate forecast profiles, asserts no coordinate fields, and the evidence validator requires `ai.model` to match the selected provider's configured model when present. OpenAI challenge generation uses Responses API structured JSON schema output, while Gemini receives the matching response schema; both server provider calls are timeout- and response-size-bounded by `FREED_AI_PROVIDER_TIMEOUT_MS` and `FREED_AI_PROVIDER_RESPONSE_MAX_BYTES`. The evidence validator rejects raw prompts, private notes, transcripts, unredacted responses, sensitive URLs/domains, and provider API keys. | Local/server fallback works; production endpoint and live model validation missing. |
| Retention: check-ins, reminders, mood/urge tracking, weekly and monthly reports | `src/lib/recovery-reminders.ts`, `src/lib/recovery-state.ts`, `src/lib/urge-risk-forecast.ts`, `src/lib/retention-orchestrator.ts`, `src/lib/remote-provider-timeout.ts`, `app/api/retention+api.ts`, `src/features/freed-app.tsx`, and tests for real-signal urge forecasting, aggregate-only retention requests, real-data weekly/monthly reporting, timeout-bounded remote-provider fallback/sanitization, route fallback, route output redaction, and server-authorized preset-copy push dispatch. | Local opt-in retention plus privacy-safe backend orchestration scaffold implemented; premium deep reports now surface monthly growth from local aggregate data; remote retention requests and JSON parsing are hard-timeout bounded, and remote push dispatch has a server-only, timeout-bounded FCM/APNs boundary, but campaigns remain disabled until production credentials, provider smoke evidence, and privacy review are complete. |
| Accountability features, privacy first | `src/lib/accountability.ts`, `src/lib/community-safety.ts`, `src/lib/recovery-state.ts`, `src/features/freed-app.tsx`, accountability/support-circle/community tests, privacy audit sponsor/support-circle report gate, and remote-community consent/abuse gate. | Local opt-in SMS/email handoff remains available for urgent support; aggregate sponsor report sending is gated by sponsor/accountability-capable plans; family/accountability-capable plans can store local support-circle contacts and open per-contact report handoff by explicit user action; report bodies still exclude private notes, contacts, and browsing details; future remote community reports fail closed until explicit consent, moderation, abuse reporting, block/mute, short retention, crisis-review, and aggregate-only rules are satisfied. |

## Monetization And Privacy

| Prompt requirement | Concrete evidence | Current state |
| --- | --- | --- |
| Ethical free/premium monetization | `src/lib/monetization.ts`, `src/lib/native-iap-adapter.ts`, `src/lib/purchase-verification.ts`, `app/api/purchases/verify+api.ts`, `scripts/purchase-verification-smoke.ts`, `src/lib/native-monetization-adapter.ts`, `src/lib/native-monetization-runtime.ts`, paywall in `src/features/freed-app.tsx`, local verified-plan metadata in `src/lib/recovery-state.ts`, and `.env.production.example` product IDs for individual, family, accountability, and AI-coach plans. | Direct native IAP, timeout- and response-size-bounded fail-closed server purchase verification with server-minted store auth tokens and timeout/response-size-bounded provider calls, purchase endpoint smoke harness, AdMob SDKs wired, purchase/restore plan metadata persistence when the verified product can be identified, and a plan capability matrix for no-ad interruptions, premium resets, custom resets, analytics, CLARA, sponsor/accountability, family support, relapse prediction, and deep reports; the app uses those capabilities for ad skipping, premium resets, local custom reset creation, CLARA+, analytics detail, urge forecasts, sponsor reports, support-circle handoff, and monthly deep reports. Real store/ad credentials and sandbox evidence with per-platform purchase/restore run IDs and artifacts is missing. RevenueCat remains optional fallback only. |
| Rewarded video ads are smooth and do not block recovery if ad fails | Rewarded session fallback tests in `tests/core.test.ts`; adapter fallback in `src/lib/native-monetization-adapter.ts`; store/ad evidence requires free rewarded intervention proof, rewarded load/request artifact, concrete loaded-ad response ID, non-personalized request mode, coarse country context, App Store/Play privacy-disclosure review ID/artifact, completion grant artifact, ad-failure fallback artifact, and premium no-ad intervention proof. | Implemented; live ad inventory validation missing. |
| Privacy-safe recovery continuity | `src/lib/recovery-backup.ts`, `src/lib/recovery-backup-client-sync.ts`, `src/lib/recovery-backup-sync.ts`, `src/lib/supabase-auth-client.ts`, `app/api/recovery-backup/sync+api.ts`, Profile encrypted backup card in `src/features/freed-app.tsx`, `docs/privacy-data-map.md`, and core tests for encrypted backup round-trip plus hosted-sync no-leak behavior. | Opt-in encrypted backup/restore implemented. Hosted continuity now has a client/server boundary: the app encrypts before upload, decrypts after download, and never sends the passphrase; the server validates Supabase Auth, accepts only the already-encrypted FREED backup envelope, rate-limits the route, stores by hashed user/device identity, and never receives decrypted recovery state. Profile sync controls stay hidden until `EXPO_PUBLIC_RECOVERY_BACKUP_SYNC_ENDPOINT` is configured; the client now supports timeout- and response-size-bounded hosted sync, Supabase public URL/anon-key auth, email magic-link handoff, Apple/Google OAuth URL handoff, and redirect access-token capture while keeping service-role/JWT secrets server-only. Privacy disclosure review and production evidence are still missing before enablement. |
| Privacy-safe analytics/personalization | `docs/privacy-data-map.md`, `src/lib/recovery-state.ts`, `src/lib/recovery-analytics.ts`, `src/lib/recovery-analytics-ingestion.ts`, `src/lib/recovery-backup-client-sync.ts`, `src/lib/recovery-backup-sync.ts`, `src/lib/backend-provider-timeout.ts`, `src/lib/backend-retention-cleanup.ts`, `src/lib/backend-architecture.ts`, `src/lib/backend-event-audit.ts`, `src/lib/backend-infrastructure.ts`, `src/lib/remote-notifications.ts`, `app/api/analytics+api.ts`, `app/api/backend/readiness+api.ts`, `app/api/backend/cleanup+api.ts`, `app/api/recovery-backup/sync+api.ts`, `app/api/notifications/send+api.ts`, `docs/backend/supabase-schema.sql`, `supabase/migrations/20260518000100_freed_backend_core.sql`, `supabase/migrations/20260520000100_harden_analytics_privacy_keys.sql`, `src/lib/urge-risk-forecast.ts`, Analytics snapshot and urge forecast cards in `src/features/freed-app.tsx`, `src/lib/ai-coach.ts`, `src/lib/challenge-generator.ts`, `src/lib/challenge-context.ts`, `scripts/privacy-safety-audit.js`, `scripts/backend-architecture-audit.js`, `scripts/client-bundle-secret-audit.js`. | Local aggregate analytics snapshot now tracks real production metrics such as hydrated active app opens, AppState-derived foreground minutes, blocked attempts, challenge completions, earned unlock totals, weekly unlock frequency, seven-day streak history, relapse resets, relapse reset rate, days since last reset, full 24-hour urge patterns, category-level challenge success rates, and recovery score; opt-in aggregate-only remote analytics transport with bounded client sends and response reads, `/api/analytics` endpoint gating, shared hashed rate limiting for analytics ingestion, timeout-bounded server-side Supabase ingestion, expanded Supabase schema and deployable migrations for feed versions/encrypted recovery backups/purchase audits/redacted AI events/job runs plus normalized analytics sensitive-key rejection, hashed purchase-verification audit persistence, redacted AI route summary persistence, shared Redis/Supabase provider timeout/response-size bounds, hashed Redis/Upstash public-route rate limits that fail closed in production when Redis is unavailable, adult-feed sync locks, sanitized backend job-run persistence, server-authorized remote notification dispatch with prewritten recovery-safe copy and route/kind metadata only, FCM readiness with `FIREBASE_PROJECT_ID` or Firebase service-account `project_id`, authenticated encrypted-backup sync with local-only passphrase handling and bounded client/server JSON reads, server-secret retention cleanup for expired rows only, and local real-signal urge forecast are implemented with private-note/browsing/contact/raw-location/passphrase exclusions and fail-closed readiness gates. Optional weather personalization is disabled by default, requires a production-safe HTTPS endpoint plus timeout/response-size bounds when enabled, uses rounded coordinates for provider lookup, reads provider JSON through bounded transport, and passes no coordinates to AI. Production `EXPO_PUBLIC_ANALYTICS_ENDPOINT`, provider disclosures/evidence, FCM/APNs dispatch credentials, and privacy review are still required before enabling remote analytics or remote push; third-party weather context, hosted backup sync, and backend providers also require production evidence before enablement. |
| No raw browsing history leakage | URL and host redaction in `src/lib/blocking-engine.ts` and `src/lib/recovery-state.ts`, CLARA redaction in `src/lib/ai-coach.ts`, Android pending intervention host-level handoff, AI safety tests. | Covered by automated tests; real native browser capture still needs validation. |

## Verification Gates

| Prompt requirement | Concrete evidence | Current state |
| --- | --- | --- |
| Run iOS simulator | Completion audit records `npx expo run:ios` and earlier `xcodebuild ... CODE_SIGNING_ALLOWED=NO` passes after native modules/monetization. Earlier Debug simulator builds with Screen Time/Safari extensions, monetization SDKs, location/sensors, and camera/photo verification passed and embedded the extension targets. Current runtime smoke installed the Debug app on iPhone 17 Pro Max, confirmed Debug needs Metro because JS bundling is skipped, started Metro on `localhost:8081`, waited through the `700823ms` cold bundle, relaunched against warm Metro, and captured a clean FREED home-screen render at `docs/validation/artifacts/local-simulator-smoke/ios-launch-warm-metro-2.png`. The repeatable `npm run smoke:ios-simulator -- --output docs/validation/artifacts/local-simulator-smoke/ios-smoke-script-rendered.png` helper warms Metro before launch, captures the rendered UI, audits the PNG for real dimensions, visible pixels, color variety, and luminance variance, scans recent simulator logs for app-specific crash/redbox signals, and writes a JSON metadata sidecar by default. After adding iOS Complete Data Protection entitlements, plist/type/privacy/test gates pass, and `npm run build:ios-simulator` now defaults to `--destination auto`, performs a bounded `xcrun simctl` available-device lookup, prints a dry-run placeholder note for unresolved `auto`, and wraps native builds with total and idle timeouts. The latest auto native rebuild passed with `npm run build:ios-simulator -- --simctl-timeout-ms 15000 --log docs/validation/artifacts/local-simulator-smoke/ios-native-build-20260524-auto.log`; it selected `iPhone 17 Pro Max` (`4E748628-BD69-48F2-B02B-9A6402FC8389`), built Debug with signing disabled into `ios/build`, validated the Shield Configuration, Shield Action, DeviceActivity Monitor, and Safari Content Blocker extensions, and ended with `** BUILD SUCCEEDED **`. | Runtime smoke and post-entitlement native simulator rebuild done; physical-device release evidence still required. |
| Run Android emulator/build | Completion audit records earlier emulator render and `./gradlew :app:assembleDebug` passing after native modules/monetization. Latest Android embedded bundle and debug build pass after adding `expo-location`, `expo-sensors`, `expo-image-picker`, and ML Kit image labeling for real challenge verification. The release bundle now exports with `--dev false --minify true`; `./gradlew :app:assembleRelease -PreactNativeArchitectures=arm64-v8a` produced `android/app/build/outputs/apk/release/app-release.apk`; and `npm run smoke:android-emulator -- --device emulator-5554 --apk android/app/build/outputs/apk/release/app-release.apk --install-fixture --clear-app-data --output docs/validation/artifacts/local-android-emulator-smoke/android-smoke-rendered-release-default-avd.png --launch-wait-ms 60000` installed, launched, screenshotted, and reported metadata for `FREED_Default_API35` on Android 15. Logcat confirmed `ReactNativeJS: Running "main"` and `Displayed ... +1s79ms` with no matching app crash/ANR lines in the checked window; the smoke harness now performs this package-specific logcat crash/ANR scan, audits the PNG for real dimensions, visible pixels, color variety, and luminance variance, and writes a JSON metadata sidecar by default. | Done for local debug/release emulator build and launch; real-device browser/store/performance evidence missing. |
| Test adult-content interception, redirects, free/premium challenge flows, rewarded ads, streaks, onboarding, permissions, false positives, performance | Automated coverage: `npm run test:core`, `npm run audit:smoke-harnesses`, `npm run eval:ai-safety`, `npm run audit:classifier`, `npm run audit:android-classifier`, `npm run audit:privacy`, `npm run audit:performance`, `npm run evidence:templates`, `npm run audit:release`; release env fail-fast coverage: `npm run preflight:release-env`, including shared rejection for unsafe `--env-file` and `FREED_RELEASE_ENV_FILE` values plus validation evidence/artifact folder env-file paths; final fail-closed orchestration: `npm run verify:release` or `npm run verify:release -- --env-file <path>`; deployed backend smoke coverage now includes `npm run smoke:backend-readiness`, `npm run smoke:supabase-schema`, `npm run smoke:adult-domain-feed`, `npm run smoke:analytics-ingestion`, and `npm run smoke:remote-notifications`; evidence artifact helpers: `npm run evidence:ios-physical-device -- --device <udid-or-name> --adult-host <real-adult-host> --app <signed-freed-app-or-ipa> --short-form-url <youtube-shorts-url> --run-id <run-id> --output-dir docs/validation/artifacts/<run-id>/ios-physical-device-capture` with local/placeholder/insecure target rejection, bounded `xcrun` device-tool commands via `--tool-timeout-ms`, and signed app/extension package proof, `npm run evidence:android-real-browser -- --device <serial> --adult-url <real-adult-url> --permission-proof --native-status-proof --dns-guard-proof --run-id <run-id> --output-dir docs/validation/artifacts/<run-id>/android-real-browser-capture` with target rejection, bounded `adb` device-tool commands via `--tool-timeout-ms`, QA screenshot capture plus nonblank PNG render audit, Android Accessibility/Usage Access diagnostic artifacts, FREED native-status screenshots/UI text in `capture-manifest.json`, and `android-real-browser-evidence-fill-template.json`, `npm run evidence:android-real-browser -- --device <serial> --scenario focused-search --focused-search-query "pornography video"` with redacted focused-search proof fields in the manifest, `npm run evidence:android-real-browser -- --device <serial> --scenario none --focused-webview-proof` with focused WebView fixture proof metadata in the manifest, `npm run evidence:android-real-browser -- --device <serial> --scenario synced-feed --adult-domain-feed-host <synced-feed-only-adult-host> --dns-guard-proof` with synced-feed Accessibility and DNS Guard proof metadata in the manifest, optional `--back-stack-check` Android handoff cleanup artifacts for `android.backStackCleanupArtifact`, `npm run evidence:normal-browsing-corpus -- --run-id <run-id>` with real external URL browser-matrix plus per-browser checklist generation and separate static classifier-corpus export, `npm run evidence:performance-profile -- --ios-device <udid-or-name> --android-device <serial>` with local/placeholder/insecure normal-URL rejection, bounded `adb`/`xcrun` metadata commands via `--tool-timeout-ms`, and `performance-profile-evidence-fill-template.json`, `npm run evidence:store-ad-sandbox -- --release-env-file <production-env-file>` with non-secret Core 3 release-env context prefill plus purchase-verification endpoint rejection for placeholder/local/private/insecure targets, URL credentials, query strings, fragments, and non-`/api/purchases/verify` paths when provided, and `npm run evidence:ai-backend-smoke -- --release-env-file <production-env-file>` with non-secret release-env context prefill plus AI endpoint rejection for placeholder/local/private/insecure targets, URL credentials, query strings, fragments, and wrong `/api/clara` / `/api/challenges` / `/api/retention` route paths when provided; external evidence gate: `npm run evidence:validation`, including Android Play policy review, deployed AI backend smoke, physical-device browser matrix proof for normal-browsing false-positive corpus checks with `isPhysicalDevice=true`, per-browser run IDs/result artifacts/pass counts/zero false positives/missed blocks, per-platform iOS/Android performance profile rows with profiler artifacts, background CPU run/artifact/percent, speed/DNS run IDs plus artifacts, and no-full-traffic-proxy proof, Core 3 purchase/restore/ad run IDs, per-flow artifacts, pass/fail counts, and real store/ad/performance runs. Draft runs can be checked before promotion with `npm run evidence:validation:draft -- docs/validation/artifacts/<run-id>/draft-evidence`, then promoted with `npm run evidence:promote -- --from docs/validation/artifacts/<run-id>/draft-evidence`. | Automated/local gates mostly pass; physical-device, normal-browsing corpus, store/ad, AI backend, policy-review, and performance evidence fail until real runs are captured. |
| Reject incomplete evidence and placeholder proof | `scripts/validation-evidence.ts`, `scripts/validation-evidence-promote.ts`, `scripts/validation-template-audit.js`, `docs/validation/README.md`, `docs/validation/evidence-runbook.md`, `docs/validation/templates/*.json`, `npm run evidence:requirements`, `npm run evidence:scaffold`, `npm run evidence:templates`, `npm run evidence:validation:draft -- <draft-folder>`, `npm run evidence:promote -- --from <draft-folder>`, tests in `tests/core.test.ts`. | Implemented; copied template text, angle-bracket placeholders like `<production-env-file>`, template env files like `.env.production.example`, remote/flag-shaped env-file proof, validation evidence/artifact folder env-file paths, unsafe remote evidence hosts such as `.internal`, reserved IP ranges, `your-*`, `sample`, or `todo`, unknown/missing scaffold/validation/promotion CLI arguments, missing artifacts, partial draft packages, raw purchase tokens, raw receipts, private customer identifiers, ad-network secrets, and store-verification credentials are rejected. Template audit also fails if the normal-browsing corpus template drifts from the current classifier corpus counts or required URL category examples, checked-in draft packages drift from `scripts/validation-evidence-specs.json`, or `docs/validation/README.md` / `docs/validation/evidence-runbook.md` drift from the shared capture-helper and report-command source. `docs/validation/evidence/` currently has no real evidence files. |

## Current Command Evidence

Latest verified in this thread:

- `npm run typecheck`: pass.
- `npm run test:core`: pass, including native photo-classifier wiring, aggregate production analytics metrics, iOS Debug AdMob config guard, and lazy notification listener startup guard.
- `npm run audit:smoke-harnesses`: `59 pass, 0 fail`, covering iOS and Android smoke harness syntax, bounded iOS native-build helper syntax/self-test, Android install QA harness syntax/self-test, Android upload-keystore setup syntax/self-test, offline crash/redbox/ANR parser self-tests, PNG screenshot analyzer syntax/self-test for nonblank render proof, evidence target/output/env-file/report-path safety syntax/self-tests for placeholder/local/insecure capture targets plus validation evidence/artifact path rejection, release verifier report-artifact validation self-test, EAS submit guard syntax/self-test for draft/internal store submission, EAS workflow audit syntax/self-test/full audit for manual internal/store artifact workflows with no live submit job, centralized Android doomscroll, web short-form, and adult-domain feed source contract self-tests, iOS/Android physical-device evidence harness self-tests, the normal-browsing corpus matrix helper self-test, performance profiling matrix helper self-test, store/ad sandbox matrix helper self-test, backend readiness smoke helper self-test, Supabase schema smoke helper self-test, Supabase deployment packet syntax/self-test, adult-domain feed smoke helper self-test, analytics ingestion smoke helper self-test, remote notification smoke helper self-test, purchase verification smoke helper self-test, AI backend smoke matrix helper self-test, and runtime data-integrity audit syntax/runtime checks.
- `npm run eval:ai-safety`: `12 pass, 0 fail`.
- `npm run smoke:ai-backend`: expected fail without production AI configuration; currently `0 pass, 4 fail` for missing `OPENAI_MODEL`/`GEMINI_MODEL`, CLARA endpoint, challenge endpoint, and challenge-personalization endpoint.
- `npm run smoke:purchase-verification`: expected fail without a deployed purchase verification endpoint; currently `0 pass, 3 fail`.
- `npm run audit:classifier`: `49 pass, 0 fail`.
- `npm run audit:android-classifier`: `15 pass, 0 fail`.
- `npm run audit:privacy`: `31 pass, 0 fail`, including the Play Data Safety and App Store App Privacy answer sheets plus the public privacy, support, and account deletion routes.
- `npm run audit:accessibility`: `6 pass, 0 fail`.
- `npm run audit:runtime-data`: `12 pass, 0 fail`.
- `npm run audit:backend`: `16 pass, 0 fail`, including deployable Supabase Edge Function cron/admin contracts for reviewed adult-domain feed sync and expired-row cleanup, secretless production endpoint URL checks, plus bounded JSON request-body checks on public backend routes.
- `npm run audit:client-bundles`: `2 pass, 0 fail`.
- `npm run audit:performance`: `28 pass, 0 fail`.
- `npm run audit:dependencies`: runs the sanitized `freed-dependency-audit-v1` wrapper around `npm audit --omit=dev --json`; current registry-backed result is `0 vulnerabilities` when npm is available.
- `npx expo-doctor`: `17/17 checks passed` after Expo SDK 55 dependency alignment, direct Expo peer cleanup, `.expo/` ignore cleanup, `expo-font` plugin declaration, removal of the invalid `newArchEnabled` app config field, and the intentional native-project config sync exception for this owned `ios/` plus `android/` workspace.
- `npm run evidence:requirements`: pass; prints the schema-versioned six required evidence specs from the validator, the machine-readable production env checklist, the canonical handoff document command list, and the current production blocker groups.
- `npm run evidence:templates`: `9 pass, 0 fail`, including iOS physical-device, Android real-browser, store/ad sandbox, AI backend smoke, performance, and normal-browsing template field examples, the checked-in `2026-05-13-release-evidence` draft package, and the README/runbook handoff-doc drift checks; normal-browsing stays aligned with the current classifier corpus, and checked-in draft JSON/requirements/capture plans plus canonical handoff docs now fail if they drift from the current evidence spec or shared command source.
- `npm run evidence:scaffold -- --run-id 2026-05-13-release-evidence --force`: pass; regenerated the QA draft package and capture checklist at `docs/validation/artifacts/2026-05-13-release-evidence/CAPTURE_PLAN.md` from the current evidence spec and shared handoff-command source, including the iOS/Android `permissionExplanationSummary` proof requirement, the canonical capture/report/validation/promotion command section, and the full backend production preflight checklist for Supabase, Redis, adult-feed, analytics, readiness, notification, store, ad, and AI configuration. These drafts are a working area only and do not satisfy release gates until filled with real evidence, draft-validated, and promoted.
- `docs/validation/README.md` and `docs/validation/evidence-runbook.md`: final verification now lists the same report-capable artifacts as `verify:release -- --artifact-dir`, including the upload-signed Android APK build, backend readiness, Supabase schema, adult-domain feed, analytics ingestion, remote notifications, purchase verification, AI backend smoke, and strict release readiness reports under `docs/validation/artifacts/<run-id>/`.
- `npm run evidence:validation`: expected fail until real release evidence is captured; currently `0 pass, 6 fail` because all six required evidence files are absent from `docs/validation/evidence/`.
- `npm run audit:release`: `30 pass, 0 warn, 14 fail` without local Android signing env loaded; with `~/.freed/android-upload/freed-upload.env` loaded, the current signed-env snapshot is `31 pass, 0 warn, 13 fail`. Production readiness gates now also require EAS/store-launch handoff config, guarded EAS submit scripts, manual EAS internal/store artifact workflows, Core 3-only launch product metadata, Expo `web.output=static`, public `/privacy`, `/support`, and `/account-deletion` routes, passing hosted legal URL availability for the public store URLs, `store/console-launch-packet.md`, `store/play-store/data-safety.md`, `store/app-store/app-privacy.md`, privacy/store policy docs, and draft-gated store submission setup, and reject missing Android upload signing env, missing local upload-keystore files, and malformed optional timeout/response-size env values directly for purchase verification, adult-domain feed sync/source fetches, analytics, Supabase/schema/hosted-backup sync, notifications, and AI transport.
- `npm audit --omit=dev`: pass with 0 vulnerabilities after overriding the vulnerable transitive `xcode -> uuid` dependency to `uuid@11.1.1`; `xcode` and `uuid.v4()` were smoke-checked after install. The release command now uses `npm run audit:dependencies` so npm/tool/registry failures are reported through the sanitized wrapper.
- `npm run verify:release -- --list`: pass; command order now includes `npm run audit:smoke-harnesses` immediately after `npm run test:core`, `npm run build:android-apk:upload-signed` after Android bundle export, `npm run smoke:backend-readiness`, `npm run smoke:supabase-schema`, `npm run smoke:adult-domain-feed`, `npm run smoke:analytics-ingestion`, and `npm run smoke:remote-notifications` after client bundle secret audit, and `npm run audit:runtime-data` plus `npm run audit:backend` before performance/dependency audits.
- `npm run preflight:release-env`: expected fail closed from the unconfigured shell; the unsigned shell snapshot is `9 pass, 22 fail`, while the current Android signing env snapshot is `10 pass, 21 fail`. Production monetization, purchase-verification, store/ad, Supabase/Redis backend infrastructure, deployed backend readiness endpoint, remote notification dispatch credentials, adult-domain feed endpoint/source configuration, aggregate analytics ingestion endpoint, and remote AI env vars are not configured, while the `server-secret-public-leakage` gate passes only because no server-only secret is currently exposed as `EXPO_PUBLIC_*`. Production native purchases and restores also fail closed before any provider grant until `EXPO_PUBLIC_PURCHASE_VERIFY_ENDPOINT` is production-safe.
- `npm run export:web`: pass; static `dist/` rendered in clean headless Chrome. A DevTools-protocol smoke clicked `Skip to Dashboard` then the Analytics tab and confirmed the `PRIVACY-SAFE SNAPSHOT` card renders with aggregate-only copy and local sharing status.
- Latest `CI=1 EXPO_NO_TELEMETRY=1 npx expo export:embed --platform android ...`: pass; Android bundle does not contain store-verification secret names.
- Earlier `./gradlew :qa-webview-fixture:assembleDebug`: pass.
- Latest Android native protection compile from `android/`: `./gradlew :freed-protection:compileDebugKotlin` passed with `BUILD SUCCESSFUL in 17s` after replacing deprecated `unsafeCheckOpNoThrow` Usage Access checks with `checkOpNoThrow`; no Kotlin source deprecation warnings remain, apart from the existing Gradle deprecation notice.
- Latest Android native debug check from `android/`: `./gradlew :app:assembleDebug` passed with `BUILD SUCCESSFUL in 1m 20s` after adding ML Kit image labeling, producing `android/app/build/outputs/apk/debug/app-debug.apk`.
- Latest Android native release check from `android/`: `./gradlew :app:assembleRelease -PreactNativeArchitectures=arm64-v8a` produced `android/app/build/outputs/apk/release/app-release.apk`.
- `node -c scripts/android-emulator-smoke.js`: pass.
- `node scripts/android-emulator-smoke.js --help`: pass.
- `node scripts/android-emulator-smoke.js --self-test`: pass; covers clean launch, app crash, app ANR, unrelated crash, and missing Play Store warning detection for the built-in logcat scanner.
- `npm run smoke:android-emulator -- --device emulator-5554 --apk android/app/build/outputs/apk/release/app-release.apk --install-fixture --clear-app-data --output docs/validation/artifacts/local-android-emulator-smoke/android-smoke-rendered-release-default-avd.png --launch-wait-ms 60000`: pass on local `FREED_Default_API35` Android 15 emulator; installed release APK and QA fixture, launched `app.freed.recovery/.MainActivity` cold in 1079 ms, captured the rendered FREED onboarding screenshot, and reported package/device/screenshot metadata. The harness now clears/scans logcat around launch, audits screenshots for nonblank rendered content, and fails future runs on package-specific crash/ANR signals.
- Filtered `adb logcat -d -t 1000` after Android release smoke showed `ReactNativeJS: Running "main"` and `Displayed app.freed.recovery/.MainActivity ... +1s79ms` with no matching app `FATAL EXCEPTION` or ANR lines. It did show `app.freed.recovery requires the Google Play Store, but it is missing`, so store/ad sandbox validation still needs a Play image or physical device.
- `node -c scripts/ios-simulator-smoke.js`: pass.
- `node scripts/ios-simulator-smoke.js --help`: pass.
- `node scripts/ios-simulator-smoke.js --self-test`: pass; covers clean launch, React Native redbox, native exception, and unrelated crash parser cases for the built-in simulator log scanner.
- Latest iOS native simulator note: earlier `xcodebuild -workspace ios/FREED.xcworkspace -scheme FREED -configuration Debug -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' CODE_SIGNING_ALLOWED=NO COMPILER_INDEX_STORE_ENABLE=NO ARCHS=arm64 ONLY_ACTIVE_ARCH=YES build` passed with `** BUILD SUCCEEDED **`, including embedded validation of `FREEDShieldAction.appex`, `FREEDShieldConfiguration.appex`, `FREEDDeviceActivityMonitor.appex`, and `FREEDSafariContentBlocker.appex`. After the iOS Complete Data Protection entitlement hardening, `npm run build:ios-simulator` defaults to auto-selecting an available simulator via bounded `xcrun simctl`, prints a dry-run placeholder note when `auto` is intentionally unresolved, then runs xcodebuild with total/idle timeouts and log-tail failures. The current auto rebuild passed with `npm run build:ios-simulator -- --simctl-timeout-ms 15000 --log docs/validation/artifacts/local-simulator-smoke/ios-native-build-20260524-auto.log`; it selected `iPhone 17 Pro Max` (`4E748628-BD69-48F2-B02B-9A6402FC8389`), built Debug with signing disabled into `ios/build`, validated the Shield Configuration, Shield Action, DeviceActivity Monitor, and Safari Content Blocker extensions, and ended with `** BUILD SUCCEEDED **` after 523162 ms. A sandboxed non-elevated attempt still failed during CoreSimulator log/device access, so local simulator build proof requires normal CoreSimulator/Xcode permissions. Latest runtime smoke installed the Debug app, confirmed the expected no-Metro redbox because Debug skips bundling, started Metro on `localhost:8081`, waited for the cold bundle to complete in `700823ms`, relaunched `app.freed.recovery` against warm Metro, rendered the FREED home UI, and captured `docs/validation/artifacts/local-simulator-smoke/ios-launch-warm-metro-2.png`. The `npm run smoke:ios-simulator` helper starts Metro, warms the iOS Expo Router bundle, launches the installed app, captures and audits `docs/validation/artifacts/local-simulator-smoke/ios-smoke-script-rendered.png`, scans recent simulator logs for app-specific crash/redbox signals, and stops Metro.

## Missing To Complete The Objective

Use `docs/validation/artifacts/2026-05-13-release-evidence/CAPTURE_PLAN.md` as the current generated handoff for the six evidence captures below.

1. Provide real production/sandbox App Store / Play Core 3 product IDs, a purchase-verification endpoint, and AdMob environment variables, then run `npm run preflight:release-env` and capture `store.releasePreflightCommand`, `store.releasePreflightRunId`, and `store.releasePreflightArtifact`; validate yearly, monthly, and lifetime in `store.launchProductSandboxMatrix` with iOS purchase/restore, Android purchase/restore, and local `purchase-verification-smoke-v1` purchase/restore artifacts inspected per matrix row, plus numeric StoreKit transaction IDs, Android purchase with a real-format `GPA.1234-5678-9012-34567` order ID and full `sha256-<64-hex-chars>` token hash, purchase/restore verification reports, non-personalized rewarded requests, coarse country context, store privacy-disclosure review, free streak-risk rewarded flow with `freePostAdChallengeLatencyMs<=5000`, rewarded load/completion, ad-failure fallback, and premium no-ad intervention with concrete run IDs, artifact references, and `premiumNoAdLatencyMs<=3000` in `docs/validation/evidence/store-ad-sandbox.json`. Add RevenueCat keys only if that fallback provider is selected.
2. Deploy privacy-reviewed CLARA and challenge endpoints with server-only `OPENAI_API_KEY` plus `OPENAI_MODEL`, optionally retaining Gemini fallback env, then run `npm run preflight:release-env`, `npm run eval:ai-safety`, and `npm run smoke:ai-backend` against those endpoints and capture `docs/validation/evidence/ai-backend-smoke.json` with `ai.releasePreflightCommand`, `ai.releasePreflightRunId`, `ai.releasePreflightArtifact`, safety eval, coach smoke, challenge smoke, no-sensitive-echo, redaction, crisis fallback, and provider fallback run IDs plus artifact references.
3. Capture iOS physical-device evidence with approved Family Controls/app-group provisioning plus concrete authorization, picker, filter, selected-app daily-limit threshold, shield, DeviceActivity, normal-browsing, Safari adult-block, Safari web short-form block, Safari/web short-form challenge handoff, Screen Time sourced earned-unlock allow/relock, and rejected non-Screen-Time earned-unlock run IDs and artifact references in `docs/validation/evidence/ios-physical-device.json`. Use `npm run evidence:ios-physical-device -- --device <udid-or-name> --adult-host <real-adult-host> --app <signed-freed-app-or-ipa> --short-form-url <youtube-shorts-url> --run-id <run-id> --output-dir docs/validation/artifacts/<run-id>/ios-physical-device-capture` to collect physical iPhone metadata, signed app/extension package proof, and the manual recording checklist; this helper refuses simulator targets plus placeholder/local/insecure normal/adult targets and non-short-form `--short-form-url` values, and its checklist now calls out `ios.safariShortFormChallengeHandoffSource=ios-safari-short-form`, `ios.safariShortFormChallengeHandoffRawPathStored=false`, `ios.safariShortFormChallengeHandoffNativeUnlockActive=false`, `ios.earnedUnlockSourceHost=screen-time-shield.freed.local`, `ios.earnedUnlockRejectedSourceHost`, `ios.earnedUnlockRejectedSelectedShieldsStayedActive=true`, and `ios.earnedUnlockRejectedAdultFilterStillActive=true`. It does not replace the required Screen Time/Shield recordings.
4. Capture Android real-browser/WebView/DNS/app-shield evidence with Chrome, Firefox, Edge, Samsung Internet, focused browser search before-navigation proof, Usage Access, WebView, below-limit configured app allow, over-limit configured app handoff, sustained short-form threshold allow/intercept, earned-unlock app allow and auto-relock, synced adult-domain feed status/classifier, DNS, back-stack, normal-browsing, and Play policy review run IDs plus artifact references in `docs/validation/evidence/android-real-browser.json`. Use `npm run evidence:android-real-browser -- --device <serial> --adult-url <real-adult-url> --permission-proof --native-status-proof --dns-guard-proof --run-id <run-id> --output-dir docs/validation/artifacts/<run-id>/android-real-browser-capture` to collect physical browser screenshots/logcat/top-activity artifacts with bounded `adb` device-tool commands, nonblank PNG render audit, Android Accessibility/Usage Access diagnostic proof, FREED Profile native-status proof, and `android-real-browser-evidence-fill-template.json` for manual review, `npm run evidence:android-real-browser -- --device <serial> --scenario focused-search --focused-search-query "pornography video"` to capture the focused-search manifest row with redacted-host/raw-query proof expectations, `npm run evidence:android-real-browser -- --device <serial> --scenario none --focused-webview-proof` to capture the QA WebView fixture field-interception artifact, `npm run evidence:android-real-browser -- --device <serial> --scenario synced-feed --adult-domain-feed-host <synced-feed-only-adult-host> --dns-guard-proof` to capture the synced adult-domain feed Accessibility artifact plus DNS Guard state/probe output, and add `--back-stack-check` to expected-block captures to create `*-after-back` screenshot/activity artifacts for `android.backStackCleanupArtifact`; this helper refuses emulator targets and does not replace the missing daily-limit app-shield, short-form threshold, earned-unlock app allow/relock, and policy-review evidence.
   Browser/adult-domain challenge completions must also prove they do not pause native app shields: run `npm run evidence:android-real-browser -- --device <serial> --adult-url <real-adult-url> --scenario none --app-scenario browser-earned-unlock --configured-app-package com.instagram.android --configured-app-label Instagram` and capture `android.browserEarnedUnlockNativeAppUnlockActive=false`, `android.browserEarnedUnlockConfiguredAppStillShielded=true`, and `android.browserEarnedUnlockAdultFilterStillActive=true`.
5. Capture real-device normal-browsing false-positive corpus evidence in `docs/validation/evidence/normal-browsing-corpus.json`, including iOS Safari plus Android Chrome, Firefox, Edge, and Samsung Internet run IDs, per-browser result artifacts, exact pass counts for 12 allowed URLs, 4 adult-term recovery searches, and 4 adult-block URLs, zero false positives, and zero missed adult blocks. Use `npm run evidence:normal-browsing-corpus -- --run-id <run-id>` to generate `normal-browsing-corpus-matrix.csv` and `normal-browsing-browser-checklist.md` from the release template's real external URL sets before manual device testing; the helper exports `classifier-corpus-static.csv` separately as classifier proof, not browser QA rows.
6. Capture physical-device battery/RAM/thermal/DNS/network evidence in `docs/validation/evidence/performance-profile.json`, using `npm run evidence:performance-profile -- --ios-device <udid-or-name> --android-device <serial>` to scaffold the per-platform profiling matrix and `performance-profile-evidence-fill-template.json` after rejecting placeholder/local/insecure normal browsing URLs and bounding device-tool calls, before adding real profiler artifacts, routing-proof artifacts, network-speed and DNS-latency run IDs plus report artifacts, no-polling proof, and no-full-traffic-proxy proof.
7. Run `npm run verify:release -- --env-file <production-env-file>` and ensure `npm run audit:release` has no failing gates.
